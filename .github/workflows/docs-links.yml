name: Documentation links

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  JULIA_VERSION: '1.12'
  JULIA_NUM_PRECOMPILE_TASKS: 2
  JULIA_PKG_PRECOMPILE_AUTO: 0

jobs:
  external-links:
    name: Check external links
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
      - uses: julia-actions/setup-julia@fa02766e078afaaf09b14210362cee14137e6a32 # v3
        with:
          version: ${{ env.JULIA_VERSION }}
      - uses: julia-actions/cache@a45e8fa8be21c18a06b7177052533149e61e9b38 # v3
      - name: Instantiate documentation environments
        run: |
          julia --project=docs --startup-file=no -e 'using Pkg; Pkg.instantiate(; julia_version_strict=true)'
          julia --project=lib/ProcessBigraphs/docs --startup-file=no -e 'using Pkg; Pkg.instantiate(; julia_version_strict=true)'
      - name: Build documentation and check links
        env:
          POTTS_DEPLOY_DOCS: 'false'
          POTTS_DOCS_LINKCHECK: 'true'
          PROCESS_BIGRAPHS_DEPLOY_DOCS: 'false'
          PROCESS_BIGRAPHS_DOCS_LINKCHECK: 'true'
        run: |
          julia --project=docs --startup-file=no docs/make.jl
          julia --project=lib/ProcessBigraphs/docs --startup-file=no lib/ProcessBigraphs/docs/make.jl
