schema_version = "1.0.0"
registry_id = "process-bigraphs-phase16-migration-v1"
phase = "16"
status = "specified"
one_production_authority_per_slice = true
silent_fallback = false

[checkpoint]
new_writer = "ProcessBigraphs Phase 16 logical checkpoint"
existing_attested_readers_retained = true
legacy_conversion = "pure versioned checksummed transactional non-destructive"
settled_boundaries_only = true
aggregate_replay = "weakest component replay class"

[[slices]]
id = "field-substrate"
status = "specified"
old_authority = "CorePotts existing field path"
target_authority = "ProcessBigraphs schedule/publication with CorePotts native engine"
differential_dimensions = ["state", "time", "operation order", "RNG", "observation", "failure", "checkpoint", "remaining trajectory"]
cutover_required = true

[[slices]]
id = "dynamic-lifecycle"
status = "specified"
old_authority = "CorePotts lifecycle scheduling path"
target_authority = "ProcessBigraphs schedule and structural transaction with CorePotts compiled request kernel"
differential_dimensions = ["cell IDs and generations", "lineage", "topology", "state", "time", "RNG", "failure", "restart"]
cutover_required = true

[[slices]]
id = "merks-assembly"
status = "specified"
old_authority = "none or test-only legacy assembly"
target_authority = "ProcessBigraphs model with CorePotts and field adapters"
differential_dimensions = ["mechanism microfixtures", "schedule", "field balance", "checkpoint"]
cutover_required = false

[[slices]]
id = "cnv-assembly"
status = "specified"
old_authority = "source-traced clean-room fixtures only"
target_authority = "ProcessBigraphs model with CorePotts and field adapters"
differential_dimensions = ["mechanism microfixtures", "schedule", "lifecycle", "relationships", "degradation", "checkpoint"]
cutover_required = false

