schema_version = "1.0.0"
registry_id = "process-bigraphs-phase16-migration-v1"
phase = "16"
status = "phase16hc_specified"
one_production_authority_per_slice = true
silent_fallback = false

[checkpoint]
status = "qualified"
new_writer = "ProcessBigraphs Phase 16 logical checkpoint"
existing_attested_readers_retained = true
legacy_conversion = "pure versioned checksummed transactional non-destructive"
settled_boundaries_only = true
aggregate_replay = "weakest component replay class"

[[slices]]
id = "field-substrate"
status = "qualified"
old_authority = "CorePotts existing field path"
target_authority = "ProcessBigraphs schedule/publication with CorePotts native engine"
differential_dimensions = ["state", "time", "operation order", "RNG", "observation", "failure", "checkpoint", "remaining trajectory"]
cutover_required = true

[[slices]]
id = "dynamic-lifecycle"
status = "specified"
old_authority = "CorePotts lifecycle scheduling path"
target_authority = "ProcessBigraphs schedule and structural transaction with CorePotts compiled request kernel"
differential_dimensions = ["cell IDs and generations", "lineage", "topology", "state", "time", "RNG", "failure", "restart"]
cutover_required = true

[[slices]]
id = "merks-assembly"
status = "qualified"
old_authority = "none or test-only legacy assembly"
target_authority = "ProcessBigraphs model with CorePotts and field adapters"
differential_dimensions = ["mechanism microfixtures", "schedule", "field balance", "checkpoint"]
cutover_required = false

[[slices]]
id = "cnv-assembly"
status = "qualified"
old_authority = "source-traced clean-room fixtures only"
target_authority = "ProcessBigraphs model with CorePotts and field adapters"
differential_dimensions = ["mechanism microfixtures", "schedule", "lifecycle", "relationships", "degradation", "checkpoint"]
cutover_required = false

[[slices]]
id = "high-level-authoring-core"
status = "specified"
old_authority = "direct StaticComposite, declaration, binding, path, and canonical lowering construction"
target_authority = "immutable CompositeModel created by the ordinary Julia compose builder"
differential_dimensions = ["semantic declarations", "canonical structure", "origin map", "fingerprints", "diagnostics", "plan", "trace", "failure", "checkpoint"]
cutover_required = true

[[slices]]
id = "merks-high-level-authoring"
status = "specified"
old_authority = "qualified bounded Merks assembly authored through direct ProcessBigraph lowering records"
target_authority = "the same qualified bounded Merks mechanisms authored through CompositeModel"
differential_dimensions = ["mechanism microfixtures", "schedule", "field balance", "canonical lowering", "origin map", "observation", "checkpoint", "rollback"]
cutover_required = true

[[slices]]
id = "cnv-high-level-authoring"
status = "specified"
old_authority = "qualified bounded CNV assembly authored through direct ProcessBigraph lowering records"
target_authority = "the same qualified bounded CNV mechanisms authored through CompositeModel"
differential_dimensions = ["reduced transitions", "full startup", "schedule", "lifecycle", "relationships", "degradation", "canonical lowering", "origin map", "checkpoint", "rollback"]
cutover_required = true

[[slices]]
id = "ordinary-tests-examples-and-docs-authoring"
status = "specified"
old_authority = "mixed direct lowering records and provisional high-level spike"
target_authority = "ordinary Julia semantic authoring with a path-and-purpose raw-IR allowlist"
differential_dimensions = ["behavioral fixtures", "doctests", "public examples", "static IR guard", "compatibility readers", "independent oracles"]
cutover_required = true
