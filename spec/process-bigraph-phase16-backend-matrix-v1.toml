schema_version = "1.0.0"
matrix_id = "process-bigraphs-phase16-backend-matrix-v1"
phase = "16"
status = "phase16c_candidate"
phase16f_status = "qualified"
phase16g_status = "qualified"
phase16h_status = "qualified"
phase16hc_status = "qualified"
allowed_statuses = ["not_started", "specified", "implemented", "qualified", "unsupported", "not_applicable"]

[[envelopes]]
id = "native-cartesian-field"
adapter = "CorePottsNativeFieldEngine"
problem = "Cartesian 2D/3D cell-centered field evolution and exchange"
precision = ["Float32", "Float64"]
CPU = "qualified"
Metal = "implemented"
ROCm = "implemented"
CUDA = "not_applicable"
replay = ["exact for declared integer schedule and RNG", "numerical for declared floating profile"]
residency = "engine-owned co-resident buffers; explicit observation/checkpoint payload"

[[envelopes]]
id = "sciml-cartesian-field"
adapter = "SciMLFieldAdapter"
problem = "bounded CPU Cartesian field advance lowered to a real SciML problem"
precision = ["Float64", "declared Float32"]
CPU = "qualified"
Metal = "unsupported"
ROCm = "unsupported"
CUDA = "not_applicable"
algorithm = "explicit injected real solver algorithm; automatic selection experimental"
options = "bounded canonical and fingerprinted"
exact_target = "solver-supported exact-target operation with reached-time verification"
continuation = "reconstruct each invocation from published canonical state"
replay = ["numerical by default", "stronger class requires a separately qualified continuation envelope"]
residency = "CPU"

[[envelopes]]
id = "independent-custom-field"
adapter = "IndependentCustomFieldAdapter"
problem = "minimal bounded CPU field advance proving open protocol"
precision = ["Float64"]
CPU = "qualified"
Metal = "unsupported"
ROCm = "unsupported"
CUDA = "not_applicable"
location = "external-style conformance fixture outside ProcessBigraphs core"
independence = "no SciML dependency or shared numerical stepping helper"
replay = ["declared by fixture; floating trajectory is numerical unless exactness is separately proven"]
residency = "CPU"

[[envelopes]]
id = "merks-source-faithful-assembly"
adapter = "ProcessBigraphsCorePotts"
problem = "Merks 2006 bounded runnable assembly"
precision = ["Float64"]
CPU = "qualified"
Metal = "unsupported"
ROCm = "unsupported"
CUDA = "not_applicable"
replay = ["exact scheduling and identity", "numerical field trajectory"]
residency = "CPU"

[[envelopes]]
id = "cnv-source-faithful-assembly"
adapter = "ProcessBigraphsCorePotts"
problem = "CNV scenario 38 bounded runnable assembly"
precision = ["Float64"]
CPU = "qualified"
Metal = "unsupported"
ROCm = "unsupported"
CUDA = "not_applicable"
replay = ["exact scheduling, lifecycle identity, and seed coordinates", "numerical field trajectory"]
residency = "CPU"
