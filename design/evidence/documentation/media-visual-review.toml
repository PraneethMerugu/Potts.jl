schema_version = "1.0.0"
status = "passed"
review_date = "2026-07-27"
reviewer = "Codex documentation auditor"
method = "rendered_site_multi_frame_review"
build_command = "julia --project=docs --startup-file=no docs/make.jl"
generator_command = "julia --project=docs --startup-file=no docs/models/render_gallery_assets.jl"
reproducibility_check = "Two consecutive generations produced identical SHA-256 values for all ten SVGs and the manifest."
limitation = "Representative start, middle, and late frames were inspected in the rendered site. Reduced-motion behavior was verified from the generated SVG media query rather than an OS-level assistive-technology session."

[[assets]]
id = "two-populations-sort"
page = "docs/src/examples/differential-adhesion.md"
rendered = true
multi_frame = true
quantitative_trace = true
accessible_alt = true
reduced_motion_fallback = true
finding = "Two population colors remain legible while actual lattice states and the heterotypic-contact statistic advance together."

[[assets]]
id = "follow-the-gradient"
page = "docs/src/examples/chemotaxis.md"
rendered = true
multi_frame = true
quantitative_trace = true
accessible_alt = true
reduced_motion_fallback = true
finding = "The field direction, saved cell shape, centroid movement, and centroid trace remain visually aligned."

[[assets]]
id = "grow-divide-retire"
page = "docs/src/examples/growth-and-division.md"
rendered = true
multi_frame = true
quantitative_trace = true
accessible_alt = true
reduced_motion_fallback = true
finding = "Early, middle, and late frames visibly distinguish daughter identities and removal of the scheduled retiring seed."

[[assets]]
id = "elongated-network"
page = "docs/src/examples/elongated-network.md"
rendered = true
multi_frame = true
quantitative_trace = true
accessible_alt = true
reduced_motion_fallback = true
finding = "Four identities remain unambiguous on closed boundaries while connected shape changes track the bounded elongation statistic."
