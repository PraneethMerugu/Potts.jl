# Split provenance

This repository was extracted from
`https://github.com/PraneethMerugu/Potts.jl` at ownership commit
`4f582068` after LocalMath documentation, tests, benchmarks, specifications,
and backend witnesses were consolidated under `lib/LocalMath/`.

The extraction preserves both the historical `lib/LocalWorksets/` ancestry and
the later `lib/LocalMath/` ancestry.
