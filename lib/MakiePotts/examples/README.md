# MakiePotts examples

`native_recipe.jl` constructs a validated render frame, uses the native Makie
recipe, adds an ordinary Makie overlay, and displays the composed figure.

Run it from the example environment after resolving the package and its Potts
dependencies:

```sh
julia --project=examples examples/native_recipe.jl
```
