var documenterSearchIndex = {"docs":
[{"location":"concepts/rng-observation-persistence/#rng-observation-persistence","page":"RNG, observation, checkpoints, and replay","title":"RNG, observation, checkpoints, and replay","text":"Support level: qualified semantic RNG, typed observation, and logical checkpoint/replay on the admitted serial boundary.\n\nSemantic RNG draws are addressed by immutable lineage: root seed, owner, event, purpose, and draw ordinal. Actor order, observer presence, and unrelated draws cannot consume a shared mutable stream and shift another component’s random sequence. Observers use an isolated namespace.\n\nAn ObserverSpec declares its identity, reconstructable observer, allowed paths, schedule, record schema, continuation, and failure policy. Observation records are outputs, not state effects.","category":"section"},{"location":"concepts/rng-observation-persistence/#Settled-checkpoint-contents","page":"RNG, observation, checkpoints, and replay","title":"Settled checkpoint contents","text":"model, structural, execution, and runtime policy identities;\ncommitted snapshot and logical time;\nprocess, step, and observer clocks;\ntyped continuations and their compatibility contracts;\nevent and observation position;\nstructural epoch and lineage when applicable;\nintegrity fingerprint and schema version.\n\nExact replay means compatible logical state and event history match the qualified contract. Numerical engine continuations may instead declare :numerical, :statistical, or :unsupported; those labels must not be upgraded by a successful run.\n\nFail-stop transactions retain the prior committed state and record the stage of failure. Restore rejects corrupt, future, or semantically incompatible archives.\n\nNext: Capability status, migration, and troubleshooting.","category":"section"},{"location":"learn/first-multirate-composite/#first-multirate-composite","page":"Your first multirate composite","title":"Your first multirate composite","text":"Support level: qualified unpublished internal beta.\n\nOutcome. Build and run two processes with different cadences against one typed store, then inspect committed state.\n\nPrerequisites. The mental model.","category":"section"},{"location":"learn/first-multirate-composite/#Complete-executed-source","page":"Your first multirate composite","title":"Complete executed source","text":"using ProcessBigraphs\nimport ProcessBigraphs: AbstractProcess, ports, semantic_version,\n    semantic_parameters, invoke\n\nstruct ScheduledPulse <: AbstractProcess\n    amount::Int\nend\n\nports(::ScheduledPulse) = (\n    InputPort(Int, :state),\n    OutputPort(Int, :out; update_law=:add),\n)\nsemantic_version(::ScheduledPulse) = \"1.0.0\"\nsemantic_parameters(pulse::ScheduledPulse) = (amount=pulse.amount,)\ninvoke(pulse::ScheduledPulse, inputs, context) = InvocationResult((\n    emit(context, :out, AdditiveUpdate(), pulse.amount),\n))\n\nscale = TimeScale(1, 1, :minute)\nmodel = compose(:FirstMultirate; scale, profile=:reproducible) do system\n    total = store!(\n        system, :total,\n        LeafSchema(Int; default=0, update_law=:add),\n    )\n\n    fast = mount!(system, :fast, ScheduledPulse(1))\n    attach!(system, fast, (state=total, out=total))\n    schedule!(system, fast, Every(Duration(1, scale)))\n\n    slow = mount!(system, :slow, ScheduledPulse(10))\n    attach!(system, slow, (state=total, out=total))\n    schedule!(system, slow, Every(Duration(2, scale)))\n\n    observable!(system, :total, total)\nend\n\nproblem = SimulationProblem(\n    model;\n    tspan=(LogicalTime(0, scale), LogicalTime(4, scale)),\n    observations=(model.observables.total,),\n    seed=2021,\n)\nruntime = initialize_runtime(problem)\nrun_until!(runtime, last(problem.tspan))\n\nresult = (\n    total=current_snapshot(runtime)[path(\"total\")],\n    events=event_count(runtime),\n    settled=settled(runtime),\n    fingerprint=problem_fingerprint(problem),\n)\n@assert result == (\n    total=24,\n    events=4,\n    settled=true,\n    fingerprint=problem_fingerprint(problem),\n)\n\nAt minutes 2 and 4 both processes are due. Their additive effects reconcile within one atomic event boundary, so six activations produce four committed events. Readers never observe a half-published batch.\n\nminute       1          2          3          4\nfast        +1         +1         +1         +1\nslow                    +10                    +10\ncommitted    1          12         13          24\n\nMaterial defaults. Exact minute scale, amounts 1 and 10, cadences 1 and 2, four-minute horizon, master seed 2021.\n\nExpected result. total == 24, four committed events, settled runtime.\n\nEstablishes. Exact multirate scheduling and deterministic additive reconciliation for this bounded serial example.\n\nDoes not establish. Logical minutes are declared units, not validated physical time; this run is not a performance benchmark.\n\nBackend / runtime / seed. CPU serial runtime, seed 2021.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/learn/first_multirate_composite.jl\n\nNext step. Learn stores, ports, schemas, and updates.","category":"section"},{"location":"concepts/architecture/#architecture-concept","page":"Architecture and compute ownership","title":"Architecture and compute ownership","text":"Support level: qualified unpublished internal beta architecture.\n\n(Image: Explicit authoring lowers to one canonical structure and execution plan; the runtime schedules external computation and publishes only validated state.)","category":"section"},{"location":"concepts/architecture/#One-owner-for-each-decision","page":"Architecture and compute ownership","title":"One owner for each decision","text":"Concern ProcessBigraphs owns Adapter or domain engine owns\nMeaning stores, ports, schedules, update laws scientific mechanism and parameters\nTime exact logical boundaries and due order numerical substeps inside authorization\nState committed snapshots and projections private workspaces and staged candidates\nPublication reconciliation, validation, atomic commit candidate construction\nPersistence logical checkpoint envelope and compatibility declared continuation codec/state\nHardware authorization and declared capability kernels, buffers, streams, devices\n\nAuthoring lowers deterministically:\n\nCompositeModel → LoweredModel → CanonicalModel + ExecutionPlan → SerialRuntime\n\nThe ACSet-backed canonical model is structural authority. The execution plan is an immutable indexed view used by runtime; it is not a second authoring model. Mutable runtime state contains clocks, continuations, projections, committed state, observations, and diagnostics—not an editable copy of structure.","category":"section"},{"location":"concepts/architecture/#Candidate-publication-is-the-seam","page":"Architecture and compute ownership","title":"Candidate publication is the seam","text":"An engine invocation receives immutable, versioned projections and explicit resource authorization. stage_operation! may perform heavy work. complete_operation! returns a candidate, early return, event request, or structured failure. A candidate becomes visible only after validation and publish_candidate!.\n\nThis boundary prevents a solver buffer or completed GPU kernel from silently becoming committed model state.","category":"section"},{"location":"concepts/architecture/#Current-boundary","page":"Architecture and compute ownership","title":"Current boundary","text":"The qualified internal beta includes the immutable-topology serial runtime, open composition, typed observations, exact logical checkpoints, atomic structural transaction primitives, managed engine handoff, and bounded scientific assemblies. It is not a public registry release or a claim of feature parity with every process-bigraph system.\n\nNext: Canonical structure and semantic identity.","category":"section"},{"location":"concepts/hierarchy-open-composition/#hierarchy-open-composition","page":"Hierarchy and open composition","title":"Hierarchy and open composition","text":"Support level: qualified static open composition.\n\nAn open composite exposes selected typed stores as import, export, or bidirectional endpoints. A parent mounts a reusable definition under an explicit namespace and joins endpoints through exact-compatible stores.\n\nparent.shared\n   ├── left.state   (mounted definition instance :left)\n   └── right.state  (mounted definition instance :right)\n\nCompatibility is exact across value type, shape, units, ontology, update law, persistence, residency, and endpoint transfer. Composition does not infer scientific conversion.\n\nHierarchy is retained in the canonical structure and flattened deterministically into one execution plan. Mount order does not determine semantic identity. The Catlab structured-cospan and annotated wiring views are inspection/interchange products; generic diagrams are not silently accepted as compilation authority.\n\nNext: Dynamic structural transactions.","category":"section"},{"location":"learn/checkpoint-failure-replay/#checkpoint-failure-replay","page":"Checkpoint, fail, restore, and replay","title":"Checkpoint, fail, restore, and replay","text":"Support level: exact logical checkpoint/replay on the admitted serial boundary.\n\nOutcome. Stop at a settled boundary, capture a checkpoint, restore against the same plan, and prove exact continuation against an uninterrupted run.\n\nPrerequisites. Change structure transactionally.","category":"section"},{"location":"learn/checkpoint-failure-replay/#Complete-executed-source","page":"Checkpoint, fail, restore, and replay","title":"Complete executed source","text":"using ProcessBigraphs\nimport ProcessBigraphs: AbstractProcess, ports, semantic_version,\n    semantic_parameters, invoke\n\nstruct ReplayIncrement <: AbstractProcess\n    amount::Int\nend\n\nports(::ReplayIncrement) = (\n    InputPort(Int, :state),\n    OutputPort(Int, :out; update_law=:add),\n)\nsemantic_version(::ReplayIncrement) = \"1.0.0\"\nsemantic_parameters(law::ReplayIncrement) = (amount=law.amount,)\ninvoke(law::ReplayIncrement, inputs, context) = InvocationResult((\n    emit(context, :out, AdditiveUpdate(), law.amount),\n))\n\nscale = TimeScale(1)\nmodel = compose(:Replayable; scale, profile=:reproducible) do system\n    state = store!(\n        system, :state,\n        LeafSchema(Int; default=0, update_law=:add),\n    )\n    actor = mount!(system, :increment, ReplayIncrement(2))\n    attach!(system, actor, (state=state, out=state))\n    schedule!(system, actor, Every(Duration(1, scale)))\nend\nplan = compile(model)\n\nuninterrupted = initialize_runtime(plan)\nrun_until!(uninterrupted, LogicalTime(5, scale))\n\ninterrupted = initialize_runtime(plan)\nrun_until!(interrupted, LogicalTime(2, scale))\nsaved = checkpoint(interrupted)\nrestored = restore(plan, saved)\nrun_until!(restored, LogicalTime(5, scale))\n\nresult = (\n    value=current_snapshot(restored)[path(\"state\")],\n    exact=snapshot_fingerprint(current_snapshot(restored)) ==\n        snapshot_fingerprint(current_snapshot(uninterrupted)),\n    events=event_count(restored),\n    checkpoint=checkpoint_fingerprint(saved),\n)\n@assert result.value == 10\n@assert result.exact\n@assert result.events == event_count(uninterrupted)\n\nCheckpoint compatibility validates the model, execution plan, logical time, continuations, observation state, and integrity envelope. Restore does not silently reinterpret an incompatible archive.\n\nFailures inside a transaction are fail-stop: no candidate becomes visible, event counters do not advance, and a settled failure checkpoint records the diagnostic. See RNG, observation, checkpoints, and replay for the full guarantee boundary.\n\nMaterial defaults. Add 2 each tick; cut after tick 2; finish at tick 5.\n\nExpected result. Final value 10 and the same snapshot fingerprint and event count as the uninterrupted run.\n\nEstablishes. Exact compatible replay for this deterministic serial model.\n\nDoes not establish. It does not promise compatibility after an undeclared semantic change or bitwise identity for numerical engine adapters.\n\nBackend / runtime / seed. CPU serial runtime; deterministic; no RNG draw.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/learn/checkpoint_failure_replay.jl\n\nNext step. Browse the complete example gallery.","category":"section"},{"location":"examples/nested-composites/#nested-composites","page":"Reusable nested composites","title":"Reusable nested composites","text":"Support level: qualified unpublished internal beta.\n\nOutcome. Mount one open definition twice under different namespaces and join both explicit endpoints to one parent store.\n\nPrerequisites. Compose and inspect a system.","category":"section"},{"location":"examples/nested-composites/#Complete-executed-source","page":"Reusable nested composites","title":"Complete executed source","text":"using ProcessBigraphs\nimport ProcessBigraphs: AbstractProcess, ports, semantic_version,\n    semantic_parameters, invoke\n\nstruct NestedIncrement <: AbstractProcess\n    amount::Int\nend\n\nports(::NestedIncrement) = (\n    InputPort(Int, :state),\n    OutputPort(Int, :out; update_law=:add),\n)\nsemantic_version(::NestedIncrement) = \"1.0.0\"\nsemantic_parameters(law::NestedIncrement) = (amount=law.amount,)\ninvoke(law::NestedIncrement, inputs, context) = InvocationResult((\n    emit(context, :out, AdditiveUpdate(), law.amount),\n))\n\nscale = TimeScale(1)\ncounter = compose(:ReusableCounter; scale) do child\n    state = store!(\n        child, :state,\n        LeafSchema(Int; default=0, update_law=:add),\n    )\n    actor = mount!(child, :increment, NestedIncrement(1))\n    attach!(child, actor, (state=state, out=state))\n    schedule!(child, actor, Every(Duration(1, scale)))\n    expose!(child, :state, state; role=:bidirectional)\nend\n\nsystem = compose(:NestedSystem; scale) do parent\n    shared = store!(\n        parent, :shared,\n        LeafSchema(Int; default=0, update_law=:add),\n    )\n    left = mount!(parent, :left, counter)\n    right = mount!(parent, :right, counter)\n    connect!(parent, left.state, shared)\n    connect!(parent, right.state, shared)\n    expose!(parent, :total, shared; role=:bidirectional)\nend\n\nruntime = initialize_runtime(compile(system))\nrun_until!(runtime, LogicalTime(3, scale))\nresult = (\n    total=current_snapshot(runtime)[path(\"shared\")],\n    structure=ir_fingerprint(lower(system)),\n    execution=plan_fingerprint(compile(system)),\n)\n@assert result.total == 6\n\n(Image: The nested-composite panel shows two increments contributing to one shared trajectory.)\n\nThe two mounts share a definition but have distinct instance identities. Flattening retains their semantic namespaces and produces one execution plan.\n\nMaterial defaults. Two children, amount 1, cadence 1, three ticks.\n\nExpected result. Shared total 6.\n\nEstablishes. Reusable hierarchy, explicit endpoints, deterministic lowering.\n\nDoes not establish. Mounting does not clone mutable runtime state at authoring time and does not infer any connection.\n\nBackend / runtime / seed. CPU serial runtime; no RNG draw.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/examples/nested_composites.jl\n\nNext step. N-way junctions.","category":"section"},{"location":"learn/compose-and-inspect/#compose-and-inspect","page":"Compose and inspect a system","title":"Compose and inspect a system","text":"Support level: qualified unpublished internal beta.\n\nOutcome. Validate, lower, compile, and inspect one model while keeping its semantic, structural, and execution identities distinct.\n\nPrerequisites. Stores, ports, schemas, and updates.","category":"section"},{"location":"learn/compose-and-inspect/#Complete-executed-source","page":"Compose and inspect a system","title":"Complete executed source","text":"using ProcessBigraphs\nimport ProcessBigraphs: AbstractProcess, ports, semantic_version,\n    semantic_parameters, invoke\n\nstruct InspectableIncrement <: AbstractProcess\n    amount::Int\nend\n\nports(::InspectableIncrement) = (\n    InputPort(Int, :state),\n    OutputPort(Int, :increment; update_law=:add),\n)\nsemantic_version(::InspectableIncrement) = \"1.0.0\"\nsemantic_parameters(law::InspectableIncrement) = (amount=law.amount,)\ninvoke(law::InspectableIncrement, inputs, context) = InvocationResult((\n    emit(context, :increment, AdditiveUpdate(), law.amount),\n))\n\nscale = TimeScale(1)\nmodel = compose(:Inspectable; scale, profile=:reproducible) do system\n    state = store!(\n        system, :state,\n        LeafSchema(Int; default=0, update_law=:add),\n    )\n    gain = parameter!(\n        system, :gain, 3;\n        units=\"dimensionless\",\n        description=\"Documented experiment parameter\",\n    )\n    actor = mount!(system, :increment, InspectableIncrement(gain.default))\n    attach!(system, actor, (state=state, increment=state))\n    schedule!(system, actor, Every(Duration(1, scale)))\n    observable!(system, :state, state)\nend\n\nvalidation = validate(model)\nlowered = lower(model)\nplan = compile(lowered)\n\nresult = (\n    summary=describe(model),\n    expanded=diagram(model),\n    valid=isempty(validation.diagnostics),\n    semantic=semantic_fingerprint(model),\n    canonical=ir_fingerprint(lowered),\n    execution=plan_fingerprint(plan),\n)\n@assert result.valid\n\ndescribe answers task-level questions. diagram exposes the expanded stores, actors, bindings, and schedules. origin_map links compiled elements back to authoring declarations when a validation diagnostic needs explanation.\n\nMaterial defaults. Reproducible profile, gain 3, cadence 1, additive state.\n\nExpected result. Empty diagnostics and three independently named fingerprints.\n\nEstablishes. Deterministic authoring-to-plan inspection.\n\nDoes not establish. Matching fingerprints across different package versions require the corresponding semantic contracts and migration policy.\n\nBackend / runtime / seed. Compile-only CPU host; no seed.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/learn/compose_and_inspect.jl\n\nNext step. Logical time, scheduling, and publication.","category":"section"},{"location":"examples/custom-engine-adapter/#custom-engine-adapter","page":"Independent custom engine adapter","title":"Independent custom engine adapter","text":"Support level: qualified public extension protocol; the example adapter itself is documentation code, not package API.\n\nOutcome. Implement the complete candidate lifecycle for an independent analytic decay engine without using ProcessBigraphs representation internals.\n\nPrerequisites. Engines, adapters, and heavy computation.","category":"section"},{"location":"examples/custom-engine-adapter/#Complete-executed-source","page":"Independent custom engine adapter","title":"Complete executed source","text":"using ProcessBigraphs\nimport ProcessBigraphs: AbstractEngineAdapter, AbstractEngineInstance,\n    AbstractCompletionHandle, EngineCapabilities, EngineDeclaration,\n    EngineInvocation, EngineCandidate, IntervalAdvance,\n    prepare_engine, stage_operation!, complete_operation!,\n    validate_candidate, publish_candidate!, discard_candidate!\n\nstruct ExponentialDecayAdapter <: AbstractEngineAdapter\n    initial::Matrix{Float64}\n    decay::Float64\nend\n\nmutable struct ExponentialDecayInstance <: AbstractEngineInstance\n    declaration::EngineDeclaration\n    published::Matrix{Float64}\n    candidate::Matrix{Float64}\n    active_invocation::Union{Nothing,String}\nend\n\nstruct DecayCompletion <: AbstractCompletionHandle\n    invocation_id::String\n    target::LogicalTime\nend\n\nfunction prepare_engine(\n    adapter::ExponentialDecayAdapter,\n    declaration::EngineDeclaration,\n)\n    declaration.adapter === adapter ||\n        throw(ArgumentError(\"declaration and adapter disagree\"))\n    ExponentialDecayInstance(\n        declaration,\n        copy(adapter.initial),\n        copy(adapter.initial),\n        nothing,\n    )\nend\n\nfunction stage_operation!(\n    instance::ExponentialDecayInstance,\n    invocation::EngineInvocation,\n)\n    invocation.operation isa IntervalAdvance ||\n        throw(ArgumentError(\"this adapter supports interval advance only\"))\n    invocation.resource_authorization == (\n        backend=:cpu,\n        precision=:float64,\n        residency=:host,\n    ) || throw(ArgumentError(\"explicit CPU/Float64/host authorization required\"))\n    isnothing(instance.active_invocation) ||\n        throw(ArgumentError(\"a candidate is already staged\"))\n\n    operation = invocation.operation\n    elapsed = physical_value(operation.target_time) -\n        physical_value(operation.start_time)\n    instance.candidate =\n        instance.published .* exp(-instance.declaration.adapter.decay * elapsed)\n    instance.active_invocation = invocation.id\n    DecayCompletion(invocation.id, operation.target_time)\nend\n\nfunction complete_operation!(\n    instance::ExponentialDecayInstance,\n    handle::DecayCompletion,\n)\n    instance.active_invocation == handle.invocation_id ||\n        throw(ArgumentError(\"completion handle does not own the candidate\"))\n    EngineCandidate(\n        handle.target,\n        copy(instance.candidate);\n        effects=(:field_state => copy(instance.candidate),),\n        diagnostics=(\n            backend=:cpu,\n            algorithm=:analytic_exponential_decay,\n            retcode=:success,\n        ),\n    )\nend\n\nfunction publish_candidate!(\n    instance::ExponentialDecayInstance,\n    invocation::EngineInvocation,\n    candidate::EngineCandidate,\n)\n    instance.active_invocation == invocation.id ||\n        throw(ArgumentError(\"candidate belongs to another invocation\"))\n    instance.published = copy(candidate.payload)\n    instance.active_invocation = nothing\n    (published=true, mass=sum(instance.published))\nend\n\nfunction discard_candidate!(\n    instance::ExponentialDecayInstance,\n    invocation::EngineInvocation,\n    candidate,\n)\n    if instance.active_invocation == invocation.id\n        instance.candidate = copy(instance.published)\n        instance.active_invocation = nothing\n    end\n    nothing\nend\n\nscale = TimeScale(1, 10, :second)\nadapter = ExponentialDecayAdapter(fill(2.0, 4, 4), 0.25)\ndeclaration = EngineDeclaration(\n    \"independent-decay\",\n    adapter;\n    semantic_version=\"1.0.0\",\n    parameters=(decay=adapter.decay, algorithm=:analytic_exponential_decay),\n    capabilities=EngineCapabilities(\n        operation_families=(:interval_advance,),\n        problem_envelopes=(\"bounded-positive-decay\",),\n        backends=(:cpu,),\n        precisions=(:float64,),\n        residencies=(:host,),\n        input_modes=(:frozen,),\n        continuation_actions=(:reconstruct,),\n        replay_class=:numerical,\n    ),\n)\ninvocation = EngineInvocation(\n    \"decay/1\",\n    :scheduled_field_advance,\n    declaration,\n    IntervalAdvance(LogicalTime(0, scale), LogicalTime(5, scale));\n    structural_epoch=\"docs-epoch-1\",\n    resource_authorization=(\n        backend=:cpu,\n        precision=:float64,\n        residency=:host,\n    ),\n    expected_outputs=(:field_state,),\n    expected_diagnostics=(:backend, :algorithm, :retcode),\n)\n\ninstance = prepare_engine(declaration)\nhandle = stage_operation!(instance, invocation)\ncandidate = complete_operation!(instance, handle)\n@assert validate_candidate(instance, invocation, candidate)\npublication = publish_candidate!(instance, invocation, candidate)\n\nresult = (\n    observed=instance.published[1, 1],\n    expected=2exp(-0.25 * 0.5),\n    publication,\n    declaration=declaration.fingerprint,\n)\n@assert isapprox(result.observed, result.expected; atol=1.0e-12)\n\n(Image: Three boxes labeled stage, validate, and publish emphasize that numerical completion is not yet visible state.)\n\nThe adapter owns its mutable instance and candidate. It cannot publish before the runtime validates reached time, effect names, diagnostics, authorization, and policy. A production executor calls these hooks inside a fail-stop transaction and invokes discard_candidate! after pre-publication failure.\n\nMaterial defaults. 4×4 field at 2.0, analytic decay 0.25, 0.5-second interval, CPU/Float64/host.\n\nExpected result. Each site equals 2exp(-0.125) within 1e-12.\n\nEstablishes. Required public dispatch points, call order, candidate validation, explicit resource authorization, and publication ownership.\n\nDoes not establish. The example adapter does not support cancellation, resize, device residency, or continuation preservation.\n\nBackend / runtime / seed. Independent CPU analytic engine; no RNG draw.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/examples/custom_engine_adapter.jl\n\nNext step. Read the extension protocol reference.","category":"section"},{"location":"concepts/capability-migration-troubleshooting/#capability-migration-troubleshooting","page":"Capability status, migration, and troubleshooting","title":"Capability status, migration, and troubleshooting","text":"Support level: this page is the operational boundary for the qualified unpublished internal beta.","category":"section"},{"location":"concepts/capability-migration-troubleshooting/#Status-vocabulary","page":"Capability status, migration, and troubleshooting","title":"Status vocabulary","text":"Label Meaning\nsupported internal beta ordinary user API retained through explicit migration\nqualified extension public dispatch protocol with conformance obligations\nexperimental internal beta inspectable but outside the admitted stable boundary\ndeprecated compatibility retained reader or call shape with a migration target\ninternal representation; downstream use is unsupported\n\nThe API pages show the owning task and support label for admitted bindings. Export alone is not a scientific or compatibility claim.","category":"section"},{"location":"concepts/capability-migration-troubleshooting/#Migration-discipline","page":"Capability status, migration, and troubleshooting","title":"Migration discipline","text":"Name the old and new semantic versions.\nRecord preserved parameters and intentional changes.\nProvide an explicit archive reader or reject with a migration-required error.\nCompare canonical meaning and bounded behavior where equivalence is claimed.\nRequalify docs, tests, oracles, performance, and browser evidence affected by the change.","category":"section"},{"location":"concepts/capability-migration-troubleshooting/#Troubleshooting","page":"Capability status, migration, and troubleshooting","title":"Troubleshooting","text":"Symptom First check\nModelValidationError read every diagnostic location and suggestion; missing schedule and binding errors may coexist\nincompatible port/store compare type, shape, units, ontology, update law, residency\nengine authorization rejected compare backend, precision, residency with declared capabilities\npartial horizon rejected choose a compatible horizon or declare partial support\ncheckpoint rejected compare schema, model, plan, continuation, and runtime policy identities\nobserver cannot read a path add the path explicitly to its ObserverSpec\nstale structure request rebuild it against the current structural epoch and generation\n\nFor defects, report the package version, Julia version, reproduction program, semantic and plan fingerprints, backend authorization, seed, and full diagnostic—without private scientific data.\n\nNext: use the API by task or return to the example gallery.","category":"section"},{"location":"examples/n-way-junctions/#n-way-junctions","page":"N-way junctions","title":"N-way junctions","text":"Support level: qualified unpublished internal beta.\n\nOutcome. Connect three named producers to one exact-compatible junction and observe deterministic additive reconciliation.\n\nPrerequisites. Reusable nested composites.","category":"section"},{"location":"examples/n-way-junctions/#Complete-executed-source","page":"N-way junctions","title":"Complete executed source","text":"using ProcessBigraphs\nimport ProcessBigraphs: AbstractProcess, ports, semantic_version,\n    semantic_parameters, invoke\n\nstruct JunctionSource <: AbstractProcess\n    amount::Int\nend\n\nports(::JunctionSource) = (\n    OutputPort(Int, :out; update_law=:add),\n)\nsemantic_version(::JunctionSource) = \"1.0.0\"\nsemantic_parameters(source::JunctionSource) = (amount=source.amount,)\ninvoke(source::JunctionSource, inputs, context) = InvocationResult((\n    emit(context, :out, AdditiveUpdate(), source.amount),\n))\n\nscale = TimeScale(1)\nmodel = compose(:NWayJunction; scale) do system\n    junction = store!(\n        system, :junction,\n        LeafSchema(Int; default=0, update_law=:add),\n    )\n    for (name, amount) in ((:left, 1), (:center, 2), (:right, 3))\n        source = mount!(system, name, JunctionSource(amount))\n        connect!(system, source.out, junction)\n        schedule!(system, source, Every(Duration(1, scale)))\n    end\n    expose!(system, :sum, junction; role=:export)\nend\n\nruntime = initialize_runtime(compile(model))\nrun_until!(runtime, LogicalTime(2, scale))\nresult = (\n    value=current_snapshot(runtime)[path(\"junction\")],\n    commits=event_count(runtime),\n    diagram=diagram(model),\n)\n@assert result.value == 12\n@assert result.commits == 2\n\n(Image: Three bars labeled plus one, plus two, and plus three summarize each source’s contribution to the junction.)\n\nThe loop reduces repetition but not semantics: every component name, amount, connection, and schedule is present at the call site. No port is selected by string similarity.\n\nMaterial defaults. Amounts 1, 2, and 3; cadence 1; two ticks.\n\nExpected result. Two publications and value 12.\n\nEstablishes. N-way exact-compatible additive junction behavior.\n\nDoes not establish. Other update laws have different conflict contracts.\n\nBackend / runtime / seed. CPU serial runtime; no RNG draw.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/examples/n_way_junctions.jl\n\nNext step. SciML field adapter.","category":"section"},{"location":"examples/pulse-and-decay/#pulse-and-decay","page":"Pulse and decay","title":"Pulse and decay","text":"Support level: qualified unpublished internal beta.\n\nOutcome. Publish additive accumulation and multiplicative retention from one scheduled process.\n\nPrerequisites. Stores, ports, schemas, and updates.","category":"section"},{"location":"examples/pulse-and-decay/#Complete-executed-source","page":"Pulse and decay","title":"Complete executed source","text":"using ProcessBigraphs\nimport ProcessBigraphs: AbstractProcess, ports, semantic_version,\n    semantic_parameters, invoke\n\nstruct PulseAndDecay <: AbstractProcess\n    pulse::Float64\n    retention::Float64\nend\n\nports(::PulseAndDecay) = (\n    OutputPort(Float64, :accumulated; update_law=:add),\n    OutputPort(Float64, :remaining; update_law=:multiply),\n)\nsemantic_version(::PulseAndDecay) = \"1.0.0\"\nsemantic_parameters(law::PulseAndDecay) = (\n    pulse=law.pulse,\n    retention=law.retention,\n)\ninvoke(law::PulseAndDecay, inputs, context) = InvocationResult((\n    emit(context, :accumulated, AdditiveUpdate(), law.pulse),\n    emit(context, :remaining, MultiplicativeUpdate(), law.retention),\n))\n\nscale = TimeScale(1)\nmodel = compose(:PulseAndDecay; scale) do system\n    accumulated = store!(\n        system, :accumulated,\n        LeafSchema(Float64; default=0.0, update_law=:add),\n    )\n    remaining = store!(\n        system, :remaining,\n        LeafSchema(Float64; default=100.0, update_law=:multiply),\n    )\n    dynamics = mount!(system, :dynamics, PulseAndDecay(2.0, 0.5))\n    attach!(system, dynamics, (\n        accumulated=accumulated,\n        remaining=remaining,\n    ))\n    schedule!(system, dynamics, Every(Duration(1, scale)))\nend\n\nruntime = initialize_runtime(compile(model))\nrun_until!(runtime, LogicalTime(3, scale))\nresult = (\n    accumulated=current_snapshot(runtime)[path(\"accumulated\")],\n    remaining=current_snapshot(runtime)[path(\"remaining\")],\n)\n@assert result == (accumulated=6.0, remaining=12.5)\n\n(Image: The accumulated series rises linearly while the retained series halves each tick.)\n\nMaterial defaults. Pulse 2, retention 0.5, initial remaining value 100, three ticks.\n\nExpected result. (accumulated = 6.0, remaining = 12.5).\n\nEstablishes. Two update-law contracts can publish in one atomic event.\n\nDoes not establish. The two stores do not interact and no kinetic model is being inferred.\n\nBackend / runtime / seed. CPU serial runtime; no RNG draw.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/examples/pulse_and_decay.jl\n\nNext step. Reusable nested composites.","category":"section"},{"location":"examples/divide-fail-recover/#divide-fail-recover","page":"Divide, fail, and recover","title":"Divide, fail, and recover","text":"Support level: templates and exact settled checkpoint/replay are qualified internal beta.\n\nOutcome. Combine a reusable cell definition, finite daughter permission, and an exact settled checkpoint cut.\n\nPrerequisites. Dynamic structure and checkpoint/replay.","category":"section"},{"location":"examples/divide-fail-recover/#Complete-executed-source","page":"Divide, fail, and recover","title":"Complete executed source","text":"using ProcessBigraphs\nimport ProcessBigraphs: AbstractProcess, ports, semantic_version,\n    semantic_parameters, invoke\n\nstruct RecoverableGrowth <: AbstractProcess\n    amount::Int\nend\n\nports(::RecoverableGrowth) = (\n    InputPort(Int, :mass),\n    OutputPort(Int, :out; update_law=:add),\n)\nsemantic_version(::RecoverableGrowth) = \"1.0.0\"\nsemantic_parameters(law::RecoverableGrowth) = (amount=law.amount,)\ninvoke(law::RecoverableGrowth, inputs, context) = InvocationResult((\n    emit(context, :out, AdditiveUpdate(), law.amount),\n))\n\nscale = TimeScale(1)\ncell = compose(:RecoverableCell; scale) do child\n    mass = store!(\n        child, :mass,\n        LeafSchema(Int; default=1, update_law=:add),\n    )\n    growth = mount!(child, :growth, RecoverableGrowth(1))\n    attach!(child, growth, (mass=mass, out=mass))\n    schedule!(child, growth, Every(Duration(1, scale)))\n    expose!(child, :mass, mass; role=:bidirectional)\nend\n\nhost = compose(:RecoveryHost; scale, profile=:reproducible) do system\n    mass = store!(\n        system, :mass,\n        LeafSchema(Int; default=1, update_law=:add),\n    )\n    mounted = mount!(system, :founder, cell)\n    connect!(system, mounted.mass, mass)\n    allow_instances!(system, :daughters, cell; capacity=4)\nend\nplan = compile(host)\n\nruntime = initialize_runtime(plan)\nrun_until!(runtime, LogicalTime(2, scale))\nsafe_cut = checkpoint(runtime)\nrun_until!(runtime, LogicalTime(4, scale))\n\nrecovered = restore(plan, safe_cut)\nrun_until!(recovered, LogicalTime(4, scale))\nresult = (\n    mass=current_snapshot(recovered)[path(\"mass\")],\n    exact=snapshot_fingerprint(current_snapshot(recovered)) ==\n        snapshot_fingerprint(current_snapshot(runtime)),\n    template=(name=:daughters, capacity=4),\n)\n@assert result.mass == 5\n@assert result.exact\n\n(Image: The recovery panel shows an uninterrupted path and a dashed restored path meeting at the same final state.)\n\nThe child definition is the declared basis for future daughters. A division request still needs explicit state, lineage, placement, and conflict policies; allow_instances! does not invent them. The safe cut demonstrates the recovery primitive that a fail-stop workflow uses after a rejected transaction.\n\nMaterial defaults. One founder, growth 1 per tick, daughter capacity 4, checkpoint at tick 2, horizon tick 4.\n\nExpected result. Final mass 5 and exact restored/uninterrupted state.\n\nEstablishes. Template declaration and exact continuation from a settled cut.\n\nDoes not establish. This bounded program does not execute a biological division or inject a production failure.\n\nBackend / runtime / seed. CPU serial runtime; no RNG draw.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/examples/divide_fail_recover.jl\n\nNext step. Audit failure and replay guarantees.","category":"section"},{"location":"concepts/engines-and-compute/#engines-and-compute","page":"Engines, adapters, and heavy computation","title":"Engines, adapters, and heavy computation","text":"Support level: qualified public extension protocol.\n\nAn EngineDeclaration binds a reconstructable adapter, semantic version, semantic parameters, and a narrow EngineCapabilities envelope. Capability is evidence scope—not everything a dependency could theoretically do.","category":"section"},{"location":"concepts/engines-and-compute/#Required-call-order","page":"Engines, adapters, and heavy computation","title":"Required call order","text":"prepare_engine\n      │\nstage_operation! ── returns completion handle\n      │\ncomplete_operation! ── returns candidate / early return / event / failure\n      │\nvalidate_candidate + runtime authorization\n      │\npublish_candidate!  OR  discard_candidate!\n\nAdapter instances, solver integrators, candidate buffers, streams, tasks, device allocations, and caches remain implementor-owned. Public protocol values carry only the information the runtime needs to authorize the handoff.\n\nAn extension must document operation families, problem envelopes, backend, precision, residency, input modes, boundary kinds, continuation actions, replay class, diagnostics, cancellation, resize, and bridges. It must reject requests outside that envelope before heavy work.\n\nUse managed_field_process for the standard scheduled field pattern, or implement the complete custom adapter example.\n\nNext: RNG, observation, checkpoints, and replay.","category":"section"},{"location":"api/user-authoring/#user-authoring-api","page":"User authoring API","title":"User authoring API","text":"Support level: supported internal beta. These names form the ordinary ProcessBigraphs 0.6 authoring boundary. The package is not yet a public release, but changes to this boundary require an explicit migration.\n\nProcessBigraphs models are assembled with named stores, mounted processes, explicit connections, and explicit schedules. A model definition is data: lower, validate, and compile inspect the same immutable meaning that the runtime executes.\n\nmodel = compose(:PulseCounter) do system\n    count = store!(system, :count, LeafSchema(Int; default=0))\n    pulse = mount!(system, :pulse, Pulse(1))\n    connect!(system, count, pulse.count, pulse.increment)\n    schedule!(system, pulse, Every(1))\nend\n\nreport = validate(model)\ncompiled = compile(model)\n\nThe example is intentionally explicit. ProcessBigraphs never guesses a connection or schedule from matching names.","category":"section"},{"location":"api/user-authoring/#Builders-and-inspection","page":"User authoring API","title":"Builders and inspection","text":"","category":"section"},{"location":"api/user-authoring/#Scheduling","page":"User authoring API","title":"Scheduling","text":"","category":"section"},{"location":"api/user-authoring/#Managed-numerical-fields","page":"User authoring API","title":"Managed numerical fields","text":"Use managed_field_process when ProcessBigraphs should own invocation and publication timing while an adapter owns the numerical method and heavy kernels. Authorization is deliberately required at construction time.\n\nfield_process = managed_field_process(\n    declaration;\n    resource_authorization=(\n        backend=:cpu,\n        precision=:float64,\n        residency=:host,\n    ),\n    subcycles_per_mcs=15,\n)\n\nThe concrete returned type is private. Mount and schedule the value through the ordinary process API.","category":"section"},{"location":"api/user-authoring/#ProcessBigraphs.CompositeModel","page":"User authoring API","title":"ProcessBigraphs.CompositeModel","text":"Immutable author-facing scientific and compositional meaning.\n\nThe value contains no live builder and is distinct from canonical ACSet IR and from the execution plan.\n\n\n\n\n\n","category":"type"},{"location":"api/user-authoring/#ProcessBigraphs.SimulationProblem","page":"User authoring API","title":"ProcessBigraphs.SimulationProblem","text":"SimulationProblem(model; initial, parameters, observations, interventions, tspan, seed)\n\nBind a reusable CompositeModel to typed initial values, parameter overrides, selected observables, exact-time interventions, a horizon, and a master seed. Construction validates handle ownership and value types without mutating model; use remake for an explicit variant.\n\n\n\n\n\n","category":"type"},{"location":"api/user-authoring/#ProcessBigraphs.compose","page":"User authoring API","title":"ProcessBigraphs.compose","text":"Schema-seeded ordinary Julia composition convenience.\n\nThis is useful while migrating an existing typed BranchSchema: every root leaf is declared through store!, and the callback receives both the builder and a typed store namespace. Nested schemas use explicit store! calls so their author-facing hierarchy remains intentional.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.store!","page":"User authoring API","title":"ProcessBigraphs.store!","text":"store!(builder, name, schema; initial)\n\nDeclare a named typed store in an open compose transaction and return its scoped handle. A supplied initial value is validated immediately. Store names are semantic identities; this operation does not mount or connect components.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.mount!","page":"User authoring API","title":"ProcessBigraphs.mount!","text":"mount!(builder, name, component; domain=:cpu, continuation=nothing)\nmount!(builder, name, composite)\n\nMount a process, step, or reusable open composite under an explicit semantic name. Mounting never connects, schedules, exposes, or otherwise autowires the component. Returned handles are valid only in the owning builder transaction.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.connect!","page":"User authoring API","title":"ProcessBigraphs.connect!","text":"Join one named store junction to one or more compatible component or mounted ports. This relationship-style spelling is equivalent to explicit pairwise connect! calls and never infers endpoints.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.attach!","page":"User authoring API","title":"ProcessBigraphs.attach!","text":"attach!(builder, component, bindings)\n\nBind every named port in bindings to an explicit store and require the attachment to cover the component exactly. Missing, duplicate, foreign, or incompatible bindings fail closed. attach! is concise syntax for explicit connections, not name-based inference.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.expose!","page":"User authoring API","title":"ProcessBigraphs.expose!","text":"expose!(builder, name, store; role)\n\nExpose a store as a typed :import, :export, or :bidirectional endpoint of an open composite. Exposure changes the reusable component interface but does not connect it to any parent.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.schedule!","page":"User authoring API","title":"ProcessBigraphs.schedule!","text":"schedule!(builder, component, schedule)\n\nAssign one explicit temporal or reactive schedule to a mounted component. Temporal processes require Every, At, or On; zero-time steps may use After dependencies. Duplicate or kind-incompatible schedules are rejected.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.iteration!","page":"User authoring API","title":"ProcessBigraphs.iteration!","text":"iteration!(builder, name, steps; mode, maximum_iterations)\n\nDeclare a named bounded or convergent zero-time iteration region over mounted steps. The region’s membership, mode, and bound participate in semantic identity and are validated before compilation.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.parameter!","page":"User authoring API","title":"ProcessBigraphs.parameter!","text":"parameter!(builder, name, default; units, description)\n\nDeclare a typed problem parameter with an explicit default and optional units and description. The returned handle can be supplied to SimulationProblem; parameter overrides do not mutate the reusable model.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.observable!","page":"User authoring API","title":"ProcessBigraphs.observable!","text":"observable!(builder, name, store)\n\nDeclare a named model-level observable that projects one typed store. A SimulationProblem selects declared observables explicitly; declaration alone does not schedule an external observer or record data.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.allow_instances!","page":"User authoring API","title":"ProcessBigraphs.allow_instances!","text":"allow_instances!(builder, name, definition; capacity)\n\nDeclare a reusable structural template and finite instance capacity. This authorizes future typed structural requests but creates no child instance, connection, numeric inheritance policy, or division side effect.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.lower","page":"User authoring API","title":"ProcessBigraphs.lower","text":"lower(model) -> LoweredModel\n\nNormalize a high-level authoring model into the canonical lowering boundary. Lowering is deterministic and construction-order invariant for semantically unordered declarations; it does not create mutable runtime state.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.compile","page":"User authoring API","title":"ProcessBigraphs.compile","text":"compile(model_or_lowered; backend=:cpu) -> CompiledComposite\n\nValidate and compile semantic structure into an immutable indexed execution plan and initial committed snapshot. Compilation fails on incomplete wiring, unsupported capabilities, invalid schedules, or unresolved semantic errors.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.validate","page":"User authoring API","title":"ProcessBigraphs.validate","text":"validate(model) -> ValidationReport\n\nReturn all currently detectable authoring diagnostics without compiling or executing the model. Diagnostics include stable codes, semantic locations, and actionable suggestions; an empty report means authoring validation passed.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.describe","page":"User authoring API","title":"ProcessBigraphs.describe","text":"describe(value)\n\nReturn a task-oriented immutable summary of an authoring value, model, or compiled artifact. describe exposes admitted semantic meaning and identities, not private representation fields.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.diagram","page":"User authoring API","title":"ProcessBigraphs.diagram","text":"diagram(model)\n\nReturn a deterministic expanded description of stores, actors, explicit bindings, schedules, mounts, and endpoints suitable for inspection and documentation. The result is an inspection view, not compilation authority.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.explain","page":"User authoring API","title":"ProcessBigraphs.explain","text":"explain(value)\n\nExplain an authoring relationship or validation result in semantic terms. Explanations expose exact coverage and expanded connections so convenience syntax remains auditable.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.remake","page":"User authoring API","title":"ProcessBigraphs.remake","text":"remake(problem; kwargs...) -> SimulationProblem\n\nConstruct a validated problem variant while preserving every unspecified field of problem. The original problem is immutable; changed inputs produce a new problem fingerprint.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.parameter_names","page":"User authoring API","title":"ProcessBigraphs.parameter_names","text":"Declare the problem-level parameters consumed by a component law.\n\nDomain packages extend this together with with_parameters. The default is parameter-free and keeps ProcessBigraphs independent of concrete solvers.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.with_parameters","page":"User authoring API","title":"ProcessBigraphs.with_parameters","text":"with_parameters(component, values)\n\nReturn a reconstructable component value with the explicitly supplied semantic parameters. Extension authors implement this together with parameter_names; silent partial or type-changing rebinding is unsupported.\n\n\n\n\n\n","category":"function"},{"location":"api/user-authoring/#ProcessBigraphs.Every","page":"User authoring API","title":"ProcessBigraphs.Every","text":"Every(cadence; first_due=cadence, supports_partial=true)\n\nDeclare an exact periodic process schedule. Cadence and first-due values use one TimeScale; nonpositive cadences and incompatible partial-horizon policies are rejected.\n\n\n\n\n\n","category":"type"},{"location":"api/user-authoring/#ProcessBigraphs.At","page":"User authoring API","title":"ProcessBigraphs.At","text":"At(times...)\n\nDeclare a finite set of exact one-shot process boundaries. Times are normalized deterministically and a process is exhausted after its last boundary.\n\n\n\n\n\n","category":"type"},{"location":"api/user-authoring/#ProcessBigraphs.On","page":"User authoring API","title":"ProcessBigraphs.On","text":"On(store)\n\nDeclare activation after a named store changes through committed publication. Reactive scheduling observes published state and never an uncommitted candidate.\n\n\n\n\n\n","category":"type"},{"location":"api/user-authoring/#ProcessBigraphs.After","page":"User authoring API","title":"ProcessBigraphs.After","text":"After(steps...)\n\nDeclare a zero-time step dependency on one or more explicitly named steps. Compilation rejects unknown dependencies and cycles, then produces deterministic reactive layers.\n\n\n\n\n\n","category":"type"},{"location":"api/user-authoring/#ProcessBigraphs.managed_field_process","page":"User authoring API","title":"ProcessBigraphs.managed_field_process","text":"managed_field_process(\n    declaration;\n    resource_authorization,\n    subcycles_per_mcs=1,\n)\n\nConstruct a scheduled process that advances a declared field engine and publishes its committed field state. resource_authorization must explicitly select a backend, precision, and residency admitted by declaration.\n\nThe concrete process type is intentionally private. Compose, inspect, and schedule the returned value through the ordinary AbstractProcess protocol.\n\n\n\n\n\n","category":"function"},{"location":"api/semantic-values/#semantic-values-api","page":"Semantic values, schemas, schedules, and effects","title":"Semantic values and identity API","text":"Support level: supported internal beta.\n\nSemantic identity answers whether two declarations mean the same thing, not whether their mutable runtime objects happen to share storage. Fingerprints are deterministic across sessions for admitted values and include the versions and parameters that affect behavior.","category":"section"},{"location":"api/semantic-values/#Inspect-schemas-without-reading-representation-fields","page":"Semantic values, schemas, schedules, and effects","title":"Inspect schemas without reading representation fields","text":"schema_at and schema_leaves are the complete supported inspection route. Do not read the concrete child-storage field of BranchSchema: that tuple is free to change without an API migration.\n\nmass_schema = schema_at(schema, path(:cell, :mass))\nfor (store_path, leaf) in schema_leaves(schema)\n    @info \"declared leaf\" store_path leaf\nend\n\nBoth accessors return immutable semantic values. Mutating a default value obtained elsewhere cannot alter a compiled model.","category":"section"},{"location":"api/semantic-values/#ProcessBigraphs.semantic_fingerprint","page":"Semantic values, schemas, schedules, and effects","title":"ProcessBigraphs.semantic_fingerprint","text":"semantic_fingerprint(model)\n\nReturn the stable identity of the high-level semantic authoring model. Scientific-family, canonical-IR, execution-plan, problem, run, and checkpoint identities remain separate contracts.\n\n\n\n\n\n","category":"function"},{"location":"api/semantic-values/#ProcessBigraphs.problem_fingerprint","page":"Semantic values, schemas, schedules, and effects","title":"ProcessBigraphs.problem_fingerprint","text":"problem_fingerprint(problem)\n\nReturn the identity of a SimulationProblem, including its model, typed initial state, parameters, observations, interventions, horizon, and root seed. It is not a run or checkpoint identity.\n\n\n\n\n\n","category":"function"},{"location":"api/semantic-values/#ProcessBigraphs.origin_map","page":"Semantic values, schemas, schedules, and effects","title":"ProcessBigraphs.origin_map","text":"origin_map(lowered_or_compiled)\n\nReturn deterministic provenance linking normalized or compiled elements to their high-level authoring declarations. Use this for diagnostics and audit traces rather than inspecting private lowering records.\n\n\n\n\n\n","category":"function"},{"location":"api/semantic-values/#ProcessBigraphs.schema_at","page":"Semantic values, schemas, schedules, and effects","title":"ProcessBigraphs.schema_at","text":"schema_at(schema, path)\n\nReturn the schema node addressed by a typed Path, failing when the path is absent or traverses through a leaf. This is the supported schema-inspection route; concrete BranchSchema fields are representation details.\n\n\n\n\n\n","category":"function"},{"location":"api/semantic-values/#ProcessBigraphs.schema_leaves","page":"Semantic values, schemas, schedules, and effects","title":"ProcessBigraphs.schema_leaves","text":"schema_leaves(schema)\n\nReturn a deterministic tuple of Path => LeafSchema pairs for every leaf in a schema tree. Ordering is canonical and does not expose concrete branch storage.\n\n\n\n\n\n","category":"function"},{"location":"examples/#examples-index","page":"Example gallery","title":"Example gallery","text":"Support level: all six programs use the qualified unpublished internal beta and execute in the pinned docs environment.\n\nThese examples are complete, standalone, and deliberately small. Each page shows every import, extension type, store, port, connection, schedule, authorization, run command, assertion, and material default.\n\n(Image: Six result panels summarize additive pulse and multiplicative decay, nested counters, an n-way junction, field decay, adapter publication, and checkpoint recovery.)\n\nProgram Question answered Warm budget\nPulse and decay How do two update laws publish? 15 s\nReusable nested composites How is a definition mounted twice? 15 s\nN-way junctions How do three explicit producers share state? 15 s\nSciML field adapter How does a real solver cross the engine boundary? 15 s\nIndependent custom engine adapter What must an extension author implement? 15 s\nDivide, fail, and recover How do templates and a settled cut support recovery? 15 s\n\nThe figures summarize bounded program output; they do not extend the claims made by the assertions.","category":"section"},{"location":"concepts/state-effects-reconciliation/#state-effects-reconciliation","page":"Logical state, effects, and reconciliation","title":"Logical state, effects, and reconciliation","text":"Support level: qualified serial reconciliation boundary.\n\nA component never receives a mutable store. It receives a projection of one committed snapshot and returns typed Delta values.\n\ncommitted snapshot\n      │ projection\n      ▼\n  invoke actors ──→ candidate deltas\n                         │\n             validate type, owner, event,\n              target, law, and capability\n                         │\n                         ▼\n                  deterministic reconcile\n                         │\n                  atomic publication\n                         ▼\n                 next committed snapshot\n\nThe leaf schema declares its update-law contract. Additive and multiplicative updates combine deterministically. Replacement and indexed/keyed updates apply their explicit conflict laws; ProcessBigraphs does not choose a winner from arrival order.\n\nFailures before publication leave the prior committed snapshot authoritative. No reader sees a partially reconciled event.","category":"section"},{"location":"concepts/state-effects-reconciliation/#Visibility","page":"Logical state, effects, and reconciliation","title":"Visibility","text":"Reactive steps consume the just-published layer according to the compiled dependency graph. Observers see only declared paths and cannot feed records back into model state. Engine candidates are not logical state until publication.\n\nNext: Time, schedules, and visibility.","category":"section"},{"location":"examples/sciml-field-adapter/#sciml-field-adapter-example","page":"SciML field adapter","title":"SciML field adapter","text":"Support level: qualified SciML extension on CPU/Float64/host.\n\nOutcome. Advance a constant reaction-diffusion field with a real Tsit5 solver and compare the bounded result with its analytic decay.\n\nPrerequisites. Integrate adapters and solvers.","category":"section"},{"location":"examples/sciml-field-adapter/#Complete-executed-source","page":"SciML field adapter","title":"Complete executed source","text":"using ProcessBigraphs\nusing OrdinaryDiffEqTsit5: Tsit5\n\nscale = TimeScale(1, 20, :second)\ninitial = fill(2.0, 5, 5)\nfield_problem = BoundedCartesianFieldProblem(\n    \"decaying-constant-field\",\n    initial;\n    diffusion=0.1,\n    decay=0.2,\n    tick_duration=0.05,\n    time_scale=scale,\n)\ndeclaration = sciml_field_declaration(\n    field_problem,\n    Tsit5();\n    algorithm_id=\"ordinarydiffeq-tsit5\",\n    solver_options=(abstol=1.0e-10, reltol=1.0e-10),\n)\nprocess = managed_field_process(\n    declaration;\n    resource_authorization=(\n        backend=:cpu,\n        precision=:float64,\n        residency=:host,\n    ),\n)\n\nmodel = compose(:DecayingField; scale) do system\n    field = store!(system, :field, LeafSchema(\n        Float64; shape=size(initial), default=initial, update_law=:replace))\n    forcing = store!(system, :forcing, LeafSchema(\n        Float64; shape=size(initial), default=zeros(size(initial)),\n        update_law=:replace))\n    weights = store!(system, :weights, LeafSchema(\n        Float64; shape=size(initial), default=ones(size(initial)),\n        update_law=:replace))\n    published = store!(system, :published, LeafSchema(\n        Float64; shape=size(initial), default=initial, update_law=:replace))\n    solver = mount!(system, :solver, process)\n    attach!(system, solver, (\n        field=field,\n        forcing=forcing,\n        decay_weights=weights,\n        field_out=field,\n        mcs_field=published,\n    ))\n    schedule!(system, solver, Every(Duration(1, scale)))\nend\n\nruntime = initialize_runtime(compile(model))\nrun_until!(runtime, LogicalTime(4, scale))\nfield = current_snapshot(runtime)[path(\"field\")]\nexpected = 2exp(-0.2 * 0.2)\nresult = (\n    observed=field[1, 1],\n    expected,\n    maximum_error=maximum(abs.(field .- expected)),\n)\n@assert result.maximum_error < 1.0e-8\n\n(Image: A smooth curve in the SciML field panel shows the spatially constant field decaying over time.)\n\nDiffusion contributes zero for a spatially constant periodic field, leaving an analytic exponential decay. This gives the numerical handoff a meaningful, independently computed check.\n\nMaterial defaults. 5×5 field at 2.0, diffusion 0.1, decay 0.2, four 0.05-second ticks, Tsit5 with 1e-10 tolerances.\n\nExpected result. Maximum error below 1e-8.\n\nEstablishes. Real solver handoff and candidate publication for this bounded analytic case.\n\nDoes not establish. General PDE convergence, other algorithms, or other backends.\n\nBackend / runtime / seed. CPU/Float64/host, deterministic, no seed.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/examples/sciml_field_adapter.jl\n\nNext step. Implement an independent custom adapter.","category":"section"},{"location":"case-studies/wortel-2021/#wortel-2021-case","page":"Wortel 2021","title":"Wortel 2021","text":"Content class: qualified source-bounded case study. Support level: qualified unpublished internal beta.\n\nOutcome. Assemble the reduced Wortel activity-CPM mechanism from ordinary PottsToolkit declarations, execute it through an explicit ProcessBigraph, and record a typed bounded-state summary.\n\nPrerequisites. Your first multirate composite, write components, and familiarity with Cellular Potts models.","category":"section"},{"location":"case-studies/wortel-2021/#Source-and-accepted-profile","page":"Wortel 2021","title":"Source and accepted profile","text":"Source: Wortel et al. (2021), DOI 10.1016/j.bpj.2021.04.036. The traced mechanism is the geometric Act memory term with a Moore neighborhood and explicit activity parameters.\n\nChoice Reduced docs profile\nDomain 24×24 lattice\nInitial cells deterministic 4×4 blocks\nActivity maximum 10, strength 20\nVolume target 16, strength 1\nTemperature 20\nCPM budget one attempt per site per MCS\nObservation cadence 2 MCS\nHorizon 4 MCS\nSeed 0x7068617365313403\nBackend CPU\n\nDeviations are the reduced domain, short horizon, and documentation seed.","category":"section"},{"location":"case-studies/wortel-2021/#Complete-executed-source","page":"Wortel 2021","title":"Complete executed source","text":"This is the entire program. The biological declarations, initialization, Act mechanism, ProcessBigraph stores, component, bindings, schedule, observer, runtime, and assertions are all visible and evaluated.\n\nusing PottsToolkit\nimport CorePotts\nimport ProcessBigraphs as PB\nimport SciMLBase\n\nconst WORTEL_SOURCE_DOI = \"10.1016/j.bpj.2021.04.036\"\nconst WORTEL_SEMANTIC_VERSION = \"1.0.0\"\n\nprofile = (\n    identity=:reduced_cpu,\n    dimensions=(24, 24),\n    cell_side=4,\n    maximum_activity=10.0f0,\n    activity_strength=20.0f0,\n    volume_strength=1.0f0,\n    temperature=20.0f0,\n    observation_every=2,\n    mcs=4,\n    seed=UInt64(0x7068617365313403),\n)\n\nmedium = Medium(:extracellular)\ncell = CellType(:endothelial)\npotts_model = PottsModel(\n    medium,\n    cell,\n    Volume(\n        cell => (\n            target=Float32(profile.cell_side^2),\n            strength=profile.volume_strength,\n        ),\n    ),\n    Adhesion(PairwiseLaw(\n        :contact_energy,\n        (medium, medium) => 0.0f0,\n        (medium, cell) => 6.0f0,\n        (cell, cell) => 2.0f0,\n    )),\n)\nactivity = Act(\n    maximum_activity=profile.maximum_activity,\n    strength=profile.activity_strength,\n    neighborhood=CorePotts.MooreTopology{2}(),\n    spacing=(1.0f0, 1.0f0),\n    algorithm=BudgetedSequentialCPM(\n        AttemptsPerSite(1);\n        temperature=profile.temperature,\n    ),\n    observation_every=profile.observation_every,\n)\n\nfunction initial_wortel_labels(profile)\n    labels = zeros(UInt64, profile.dimensions)\n    pitch = profile.cell_side + 2\n    identity = UInt64(0)\n    for y in 2:pitch:(profile.dimensions[2] - profile.cell_side),\n            x in 2:pitch:(profile.dimensions[1] - profile.cell_side)\n        identity += UInt64(1)\n        labels[\n            x:(x + profile.cell_side - 1),\n            y:(y + profile.cell_side - 1),\n        ] .= identity\n    end\n    labels\nend\nlabels = initial_wortel_labels(profile)\nidentities = Tuple(\n    id => cell for id in sort!(collect(Set(filter(!iszero, labels))))\n)\nbase_problem = PottsProblem(\n    potts_model,\n    CartesianDomain(profile.dimensions; spacing=(1.0f0, 1.0f0)),\n    Layout(LabelledCells(labels, identities));\n    capacity=length(identities),\n    tspan=(0, profile.mcs),\n    seed=profile.seed,\n)\nactivity_problem = CorePotts.ActivityPottsProblem(\n    base_problem,\n    lower(activity),\n)\n\nfunction activity_arrays(problem, dimensions, mcs)\n    integrator = SciMLBase.init(problem)\n    SciMLBase.step!(integrator, mcs)\n    state = CorePotts.logical_state(integrator)\n    labels = zeros(UInt64, dimensions)\n    activity = zeros(Float32, dimensions)\n    for site in eachindex(labels)\n        owner = CorePotts.owner_at(state, site)\n        labels[site] = CorePotts.is_cell_owner(owner) ?\n            UInt64(CorePotts.value(CorePotts.cell_id(owner))) : UInt64(0)\n        activity[site] =\n            Float32(CorePotts.site_property_value(integrator, site))\n    end\n    return labels, activity, CorePotts.current_mcs_report(integrator)\nend\n\nstruct WortelActivityBatch{P,T} <: PB.AbstractProcess\n    problem::P\n    profile::T\n    model_fingerprint::String\nend\n\nPB.ports(::WortelActivityBatch) = (\n    PB.PortSpec(Array{UInt64,2}, :labels, :output; update_law=:replace),\n    PB.PortSpec(Array{Float32,2}, :activity, :output; update_law=:replace),\n)\nPB.semantic_version(::WortelActivityBatch) = WORTEL_SEMANTIC_VERSION\nPB.semantic_parameters(batch::WortelActivityBatch) = (\n    family=:Wortel2021,\n    source_doi=WORTEL_SOURCE_DOI,\n    profile=batch.profile.identity,\n    potts_fingerprint=batch.model_fingerprint,\n)\nfunction PB.invoke(batch::WortelActivityBatch, inputs, context)\n    labels, activity, report = activity_arrays(\n        batch.problem,\n        batch.profile.dimensions,\n        batch.profile.mcs,\n    )\n    PB.InvocationResult((\n        PB.emit(context, :labels, PB.ReplaceUpdate(), labels),\n        PB.emit(context, :activity, PB.ReplaceUpdate(), activity),\n    ); diagnostics=(\n        accepted_copies=report.accepted_copies,\n        profile=batch.profile.identity,\n    ))\nend\n\nscale = PB.TimeScale(1, 1, :mcs)\ncomposite_model = PB.compose(\n    :Wortel2021;\n    scale,\n    profile=:reproducible,\n) do system\n    label_store = PB.store!(\n        system, :labels,\n        PB.LeafSchema(\n            UInt64;\n            shape=profile.dimensions,\n            default=zeros(UInt64, profile.dimensions),\n            update_law=:replace,\n            ontology=\"CPM cell identity\",\n        ),\n    )\n    activity_store = PB.store!(\n        system, :activity,\n        PB.LeafSchema(\n            Float32;\n            shape=profile.dimensions,\n            default=zeros(Float32, profile.dimensions),\n            update_law=:replace,\n            ontology=\"Act memory\",\n        ),\n    )\n    simulation = PB.mount!(\n        system,\n        :activity_cpm,\n        WortelActivityBatch(\n            activity_problem,\n            profile,\n            semantic_fingerprint(potts_model).digest,\n        ),\n    )\n    PB.attach!(\n        system,\n        simulation,\n        (labels=label_store, activity=activity_store),\n    )\n    PB.schedule!(\n        system,\n        simulation,\n        PB.At(PB.LogicalTime(profile.mcs, scale)),\n    )\n    PB.observable!(system, :labels, label_store)\n    PB.observable!(system, :activity, activity_store)\nend\n\nstruct WortelSummary <: PB.AbstractObserver end\nPB.observer_semantic_version(::WortelSummary) = WORTEL_SEMANTIC_VERSION\nPB.observer_semantic_parameters(::WortelSummary) = (\n    family=:Wortel2021,\n    observation=:bounded_state_summary,\n)\nfunction PB.observe(::WortelSummary, projection, context)\n    labels = projection[PB.path(\"labels\")]\n    activity = projection[PB.path(\"activity\")]\n    PB.ObservationResult((\n        mcs=Int(context.time.tick),\n        cells=length(Set(filter(!iszero, labels))),\n        occupied=count(!iszero, labels),\n        active_sites=count(>(0), activity),\n        activity_mass=sum(activity),\n    ))\nend\n\nobserver = PB.ObserverSpec(\n    \"wortel-state-summary\",\n    WortelSummary(),\n    (PB.path(\"labels\"), PB.path(\"activity\")),\n    PB.AtTimesObservationSchedule((\n        PB.LogicalTime(profile.mcs, scale),\n    ));\n    record_schema=PB.RecordSchema(\n        NamedTuple; identity=\"wortel-state-summary-v1\"),\n)\nruntime = PB.initialize_runtime(\n    PB.compile(composite_model),\n    PB.SerialExecutor(\n        root_seed=profile.seed,\n        observation_plan=PB.ObservationPlan((observer,)),\n    ),\n)\nPB.run_until!(runtime, PB.LogicalTime(profile.mcs, scale))\n\nrecord = only(PB.observation_records(runtime)).payload\nresult = (\n    record,\n    model_fingerprint=semantic_fingerprint(potts_model).digest,\n    composite_fingerprint=PB.semantic_fingerprint(composite_model),\n    source_doi=WORTEL_SOURCE_DOI,\n)\n@assert result.record.mcs == profile.mcs\n@assert result.record.cells == length(identities)\n@assert result.record.occupied > 0","category":"section"},{"location":"case-studies/wortel-2021/#State,-trace,-and-inspection-loop","page":"Wortel 2021","title":"State, trace, and inspection loop","text":"(Image: A paper-inspired native MakiePotts time strip shows the same fixed-seed reduced Act-CPM trajectory at MCS 0, 30, and 60, with white cell boundaries over the black-to-green-to-yellow-to-red activity field.)\n\n(Image: A native Makie trace shows occupied and active Wortel sites across the longer visualization profile, sampled every five MCS through MCS 60.)\n\n<div class=\"pb-native-animation\">\n<video autoplay loop muted playsinline controls preload=\"metadata\"\n       width=\"1800\" height=\"820\"\n       poster=\"../../assets/wortel-state.png\"\n       aria-label=\"Native MakiePotts animation of the fixed-seed reduced Wortel trajectory from MCS zero through sixty\">\n  <source src=\"../../assets/wortel-animation.mp4\" type=\"video/mp4\">\n  <a href=\"../../assets/wortel-animation.mp4\">Download the Wortel Makie animation.</a>\n</video>\n<img class=\"pb-animation-fallback\"\n     src=\"../../assets/wortel-state.png\"\n     alt=\"Static MakiePotts fallback showing Wortel cell identities and Act memory at MCS zero, thirty, and sixty.\">\n</div>\n\nText alternative for the animation: thirteen native MakiePotts states sample the declared seed every 5 MCS from initialization through MCS 60. White cell boundaries evolve over an activity field that runs from black (inactive) through green and yellow to red (most active). Reduced-motion users receive the three-panel MCS 0/30/60 time strip. The executed four-MCS program above remains the fast, fully displayed tutorial and typed-record authority; the longer media profile changes only the observation horizon and sampling cadence so that the stochastic dynamics are legible during a presentation.","category":"section"},{"location":"case-studies/wortel-2021/#What-this-establishes","page":"Wortel 2021","title":"What this establishes","text":"The displayed program establishes that the reduced profile:\n\nconstructs the intended Potts volume/contact model and explicit Act mechanism;\nuses the supported ActivityPottsProblem and SciML lifecycle;\npublishes labels and activity through a typed ProcessBigraph component;\nrecords cell count, occupied sites, active sites, and activity mass at 4 MCS;\nreproduces the same stochastic trajectory for the stated seed on CPU; and\nshares the packaged family’s Potts semantic fingerprint and frozen bounded behavior check.","category":"section"},{"location":"case-studies/wortel-2021/#What-this-does-not-establish","page":"Wortel 2021","title":"What this does not establish","text":"It does not reproduce Figure 2, execute the paper’s 51-parameter × 30-seed study, estimate an ensemble or output distribution, validate physical time, establish backend equivalence from this docs run, or imply endorsement by the source authors. Exact package/tutorial equality is fixed-seed reproducibility, not a claim that one stochastic trajectory characterizes the model. Separate retained qualification covers the same Act mechanism on CPU, Metal, and ROCm at its frozen evidence boundary; this page executes CPU only.\n\nMaterial defaults. All values are listed in the accepted-profile table and in profile at the top of the program.\n\nExpected result. One observation at 4 MCS with the initialized cell count, positive occupied sites, deterministic fingerprints, and a complete MCS report.\n\nBackend / runtime / seed. CPU; four bounded MCS; seed 0x7068617365313403.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/case_studies/wortel_2021.jl\n\nFull qualification command. julia --project=. -e 'using Pkg; Pkg.test(\"PottsToolkit\")'\n\nNext step. Compare the more tightly coupled field/CPM assembly in Merks 2006.","category":"section"},{"location":"learn/write-components/#write-components","page":"Write processes, steps, and observers","title":"Write processes, steps, and observers","text":"Support level: qualified extension API within the internal beta.\n\nOutcome. Implement one temporal process, one zero-time reactive step, and one typed observer using the public extension protocols.\n\nPrerequisites. Logical time and publication.","category":"section"},{"location":"learn/write-components/#Complete-executed-source","page":"Write processes, steps, and observers","title":"Complete executed source","text":"using ProcessBigraphs\nimport ProcessBigraphs: AbstractProcess, AbstractStep, AbstractObserver,\n    ports, semantic_version, semantic_parameters, invoke,\n    observer_semantic_version, observer_semantic_parameters, observe\n\nstruct ProducedSignal <: AbstractProcess end\nports(::ProducedSignal) = (\n    OutputPort(Int, :out; update_law=:add),\n)\nsemantic_version(::ProducedSignal) = \"1.0.0\"\ninvoke(::ProducedSignal, inputs, context) = InvocationResult((\n    emit(context, :out, AdditiveUpdate(), 1),\n))\n\nstruct CopySignal <: AbstractStep end\nports(::CopySignal) = (\n    InputPort(Int, :input),\n    OutputPort(Int, :out; update_law=:replace),\n)\nsemantic_version(::CopySignal) = \"1.0.0\"\ninvoke(::CopySignal, inputs, context) = InvocationResult((\n    emit(context, :out, ReplaceUpdate(), inputs[:input]),\n))\n\nstruct SignalObserver <: AbstractObserver end\nobserver_semantic_version(::SignalObserver) = \"1.0.0\"\nobserver_semantic_parameters(::SignalObserver) = (record=:signal_and_tick,)\nobserve(::SignalObserver, projection, context) = ObservationResult((\n    signal=projection[path(\"signal\")],\n    tick=Int(context.time.tick),\n))\n\nscale = TimeScale(1)\nmodel = compose(:WrittenComponents; scale) do system\n    signal = store!(\n        system, :signal,\n        LeafSchema(Int; default=0, update_law=:add),\n    )\n    copied = store!(\n        system, :copied,\n        LeafSchema(Int; default=0, update_law=:replace),\n    )\n    producer = mount!(system, :producer, ProducedSignal())\n    connect!(system, producer.out, signal)\n    schedule!(system, producer, Every(Duration(1, scale)))\n    copier = mount!(system, :copier, CopySignal())\n    attach!(system, copier, (input=signal, out=copied))\nend\n\nobserver = ObserverSpec(\n    \"signal-observer\",\n    SignalObserver(),\n    (path(\"signal\"),),\n    EventObservationSchedule();\n    record_schema=RecordSchema(\n        NamedTuple; identity=\"signal-record-v1\",\n    ),\n)\nexecutor = SerialExecutor(\n    root_seed=7,\n    observation_plan=ObservationPlan((observer,)),\n)\nruntime = initialize_runtime(compile(model), executor)\nrun_until!(runtime, LogicalTime(3, scale))\n\nresult = (\n    signal=current_snapshot(runtime)[path(\"signal\")],\n    copied=current_snapshot(runtime)[path(\"copied\")],\n    observations=Tuple(record.payload for record in observation_records(runtime)),\n)\n@assert result.signal == result.copied == 3\n@assert last(result.observations).signal == 3\n\nA process has elapsed logical time and needs an explicit schedule. A step runs inside a zero-time reactive layer when its inputs change. An observer receives only its declared projection and writes a validated record outside model state. All three types have stable reconstructable identities; no closure participates in semantic identity.\n\nMaterial defaults. Cadence 1, additive producer, event observer, root seed 7.\n\nExpected result. signal == copied == 3; the last record reports 3.\n\nEstablishes. Public component protocol, reactive visibility, typed records.\n\nDoes not establish. This stateless example does not teach continuation migration; see RNG, observation, checkpoints, and replay.\n\nBackend / runtime / seed. CPU serial runtime, seed 7.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/learn/write_components.jl\n\nNext step. Integrate adapters and solvers.","category":"section"},{"location":"api/runtime-checkpoint/#runtime-checkpoint-api","page":"Process, step, observer, runtime, and checkpoint API","title":"Runtime, observation, and checkpoint API","text":"Support level: supported internal beta.\n\nRuntime publication is transactional: an event either publishes every authorized effect or publishes none. Observations read committed boundaries, and settled checkpoints capture only replay-safe state.\n\nruntime = initialize_runtime(problem)\nrun_until!(runtime, LogicalTime(20, problem.scale))\n\nrecords = observation_records(runtime)\nsaved = checkpoint(runtime)\nresumed = restore(saved, problem)\n\nCheckpoint restoration validates model identity, schedule identity, continuation schemas, and structural position before returning a runtime. There is no “best effort” restore mode on the exact path.\n\nFor downstream scientific façades such as CorePotts.ActivityPottsProblem, the same generic operation names are extended. This keeps persistence code independent of coupled runtime representations.","category":"section"},{"location":"api/runtime-checkpoint/#ProcessBigraphs.observation_records","page":"Process, step, observer, runtime, and checkpoint API","title":"ProcessBigraphs.observation_records","text":"observation_records(runtime)\n\nReturn an immutable copy of the runtime’s published ObservationRecord sequence in event order. Records are outside logical model state and contain only the projections authorized by each observer specification.\n\n\n\n\n\n","category":"function"},{"location":"api/runtime-checkpoint/#ProcessBigraphs.checkpoint","page":"Process, step, observer, runtime, and checkpoint API","title":"ProcessBigraphs.checkpoint","text":"checkpoint(runtime) -> SettledCheckpoint\n\nCapture an integrity-checked logical checkpoint at a settled serial boundary. The checkpoint records model and plan identity, committed state, logical time, clocks, continuations, observations, and replay policy; unsettled capture fails.\n\n\n\n\n\n","category":"function"},{"location":"api/runtime-checkpoint/#ProcessBigraphs.restore","page":"Process, step, observer, runtime, and checkpoint API","title":"ProcessBigraphs.restore","text":"restore(compiled, checkpoint) -> SerialRuntime\nrestore(compiled, executor, checkpoint) -> SerialRuntime\n\nValidate and restore a compatible settled logical checkpoint. Restore rejects corrupt payloads and mismatched model, execution, continuation, observation, or runtime-policy identities rather than silently migrating them.\n\n\n\n\n\n","category":"function"},{"location":"learn/mental-model/#mental-model","page":"The process-bigraph mental model","title":"The process-bigraph mental model","text":"Support level: qualified unpublished internal beta.\n\nOutcome. See the complete path from typed stores and ports to one validated canonical structure and an indexed execution plan.\n\nPrerequisites. Install and verify.","category":"section"},{"location":"learn/mental-model/#Five-explicit-ideas","page":"The process-bigraph mental model","title":"Five explicit ideas","text":"A store declares persistent typed state.\nA component declares ports and behavior.\nA connection binds one named port to one compatible store.\nA schedule says when a temporal process becomes due.\nCompilation lowers this authoring meaning to one canonical structure and one execution plan. Runtime state is not another model authority.\n\n(Image: The authoring model lowers to a canonical model and execution plan; runtime and an engine adapter meet only through candidate publication.)","category":"section"},{"location":"learn/mental-model/#Complete-executed-source","page":"The process-bigraph mental model","title":"Complete executed source","text":"using ProcessBigraphs\nimport ProcessBigraphs: AbstractProcess, ports, semantic_version,\n    semantic_parameters, invoke\n\nstruct MentalPulse <: AbstractProcess\n    amount::Int\nend\n\nports(::MentalPulse) = (\n    InputPort(Int, :current),\n    OutputPort(Int, :increment; update_law=:add),\n)\nsemantic_version(::MentalPulse) = \"1.0.0\"\nsemantic_parameters(pulse::MentalPulse) = (amount=pulse.amount,)\ninvoke(pulse::MentalPulse, inputs, context) = InvocationResult((\n    emit(context, :increment, AdditiveUpdate(), pulse.amount),\n))\n\nscale = TimeScale(1)\nmodel = compose(:MentalModel; scale) do system\n    count = store!(\n        system, :count,\n        LeafSchema(Int; default=0, update_law=:add),\n    )\n    pulse = mount!(system, :pulse, MentalPulse(2))\n    connect!(system, pulse.current, count)\n    connect!(system, pulse.increment, count)\n    schedule!(system, pulse, Every(Duration(1, scale)))\n    observable!(system, :count, count)\nend\n\nreport = validate(model)\nplan = compile(model)\nresult = (\n    valid=isempty(report.diagnostics),\n    model=semantic_fingerprint(model),\n    structure=structural_fingerprint(plan),\n    execution=plan_fingerprint(plan),\n)\n\nmount! did not connect or schedule pulse; each relationship is visible. The three fingerprints remain distinct because authoring identity, canonical structure, and execution policy answer different questions.\n\nMaterial defaults. Integer additive state, cadence 1, amount 2, interactive authoring profile.\n\nExpected result. Validation is empty and each semantic layer has a stable identity.\n\nEstablishes. Explicit authoring and deterministic lowering.\n\nDoes not establish. No runtime or numerical-backend claim.\n\nBackend / runtime / seed. CPU host; compile only; no seed.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/learn/mental_model.jl\n\nNext step. Build the first multirate composite.","category":"section"},{"location":"learn/dynamic-structure/#dynamic-structure","page":"Change structure transactionally","title":"Change structure transactionally","text":"Support level: template authoring is supported internal beta; structural mutation remains explicitly transaction-bounded.\n\nOutcome. Declare a reusable child definition and a capacity-bounded instance template without instantiating children as a side effect of authoring.\n\nPrerequisites. Integrate adapters and solvers.","category":"section"},{"location":"learn/dynamic-structure/#Complete-executed-source","page":"Change structure transactionally","title":"Complete executed source","text":"using ProcessBigraphs\n\nscale = TimeScale(1)\ncell = compose(:CellDefinition; scale) do child\n    state = store!(\n        child, :state,\n        LeafSchema(Int; default=0, update_law=:add),\n    )\n    expose!(child, :state, state; role=:bidirectional)\nend\n\nhost = compose(:DynamicHost; scale, profile=:reproducible) do system\n    shared = store!(\n        system, :shared,\n        LeafSchema(Int; default=0, update_law=:add),\n    )\n    allow_instances!(system, :cells, cell; capacity=8)\n    observable!(system, :shared, shared)\nend\n\nreport = validate(host)\nplan = compile(host)\nresult = (\n    valid=isempty(report.diagnostics),\n    model=semantic_fingerprint(host),\n    structure=structural_fingerprint(plan),\n    template_policy=(name=:cells, capacity=8),\n)\n@assert result.valid\n\nA template is permission and schema, not a hidden child. Runtime requests created by spawn, divide, move, or remove identify their source structural epoch. Selection, reference validation, rewriting, numeric validation, and publication either produce one new immutable epoch or leave the prior epoch unchanged.\n\nrequests → deterministic selection → candidate rewrite → full validation\n                                                         │\n                                  reject (old epoch) ←───┼──→ publish (new epoch)\n\nMaterial defaults. Child endpoint is bidirectional; host capacity is eight; no initial child exists.\n\nExpected result. Valid host, stable model and structure identities, explicit template policy.\n\nEstablishes. Public authoring of reusable definitions and finite instance permission.\n\nDoes not establish. This compile-only tutorial does not execute a biological division policy or claim that numeric state can be inferred during a division.\n\nBackend / runtime / seed. Compile-only CPU host; no seed.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/learn/dynamic_structure.jl\n\nNext step. Checkpoint, fail, restore, and replay.","category":"section"},{"location":"concepts/structural-transactions/#structural-transactions","page":"Dynamic structural transactions","title":"Dynamic structural transactions","text":"Support level: atomic structural transaction primitives are qualified; domain policies remain explicit.\n\nDynamic structure changes through typed requests addressed to one source epoch. Requests may add, divide, remove, move, or rewire. Each carries a stable identity, dependencies, priority, and typed targets.\n\nsource epoch\n    │ requests\n    ▼\ndeterministic selection ── conflicts/capacity → dispositions\n    │\nreference validation → rewrite candidate → structural + numeric validation\n    │\n    ├── failure: source epoch remains authoritative\n    └── success: publish one immutable successor epoch + lineage\n\nA division cannot guess numeric inheritance, geometry, field deposition, lineage, or child connection policy. allow_instances! declares a reusable definition and finite capacity; spawn and divide author requests only when the caller supplies the required structural identities and policies.\n\nCheckpoints record epoch identity and lineage. Stale requests, stale candidates, generation mismatches, incomplete owned closures, cycles, and capacity overruns fail closed.\n\nNext: Engines, adapters, and heavy computation.","category":"section"},{"location":"case-studies/#case-studies-index","page":"Scientific case-study boundary","title":"Scientific case-study boundary","text":"Content class: qualified source-bounded case studies in a qualified unpublished internal beta.\n\nThese pages demonstrate how a scientific model can be assembled with the ordinary PottsToolkit and ProcessBigraphs APIs. They show complete source inline. They do not import a prebuilt reference-model constructor, hide scientific setup, or claim to reproduce a paper figure.\n\nCase Source-bounded mechanism Teaching profile Explicit exclusion\nWortel 2021 geometric Act memory in an activity CPM 24×24, 4 MCS, fixed CPU seed Figure 2’s 51-parameter/30-seed study\nMerks 2006 adhesion, elongation, chemotaxis, connectivity, secretion/diffusion/decay 20×20, 2 cells, 2 MCS Figure 5 morphometry and ensemble analysis\n\n(Image: Two panels show the 60-MCS fixed-seed Wortel activity and Merks field-mass visualization profiles.)","category":"section"},{"location":"case-studies/#How-to-read-the-evidence","page":"Scientific case-study boundary","title":"How to read the evidence","text":"Each page separates:\n\nsource citation and traced mechanism;\naccepted reduced profile and deviations from the canonical profile;\ncomplete executable assembly;\ntyped observations and deterministic figures;\nsemantic/equivalence checks against the packaged model family;\nadmitted conclusions and explicit nonclaims.\n\nThe paper authors have not endorsed these software reconstructions.","category":"section"},{"location":"concepts/time-schedules-visibility/#time-schedules-visibility","page":"Time, schedules, and visibility","title":"Time, schedules, and visibility","text":"Support level: qualified normalized-integer logical time and serial scheduling.\n\nTimeScale is an exact rational unit. LogicalTime is a point; Duration is an interval. Conversion is allowed only when scales have an exact common representation.\n\nAuthoring schedule Meaning\nEvery(duration) periodic temporal process\nAt(times...) finite exact one-shot boundaries\nOn(store) activation after committed publication changes a store\nAfter(steps...) zero-time reactive dependency layer\n\nAt one logical boundary, the runtime determines the complete due batch, captures authorized projections, invokes processes, validates and reconciles their effects, publishes atomically, evaluates reactive layers, records observations, and settles.\n\nt=1 due → publish → observe → settled\nt=2 due process A + B → one reconciled publish → reactive layers → observe\n\nExactHorizon may require partial-interval support to reach a noncadence target. StopPrior stops before an unsupported partial boundary. Neither policy rounds floating time.\n\nLogical units are declarations, not evidence that one tick equals a measured physical duration.\n\nNext: Hierarchy and open composition.","category":"section"},{"location":"learn/install-and-verify/#install-and-verify","page":"Install and verify the internal beta","title":"Install and verify the internal beta","text":"Support level: qualified unpublished internal beta.\n\nOutcome. Verify the local package, exact logical-time arithmetic, canonical encoding, and semantic identity in one standalone program.\n\nPrerequisites. Julia 1.12 or a later Julia 1.x release and a checkout of this repository.\n\njulia --project=lib/ProcessBigraphs/docs -e 'using Pkg; Pkg.instantiate()'","category":"section"},{"location":"learn/install-and-verify/#Complete-executed-source","page":"Install and verify the internal beta","title":"Complete executed source","text":"The following is the complete canonical program. It has no hidden scientific setup and the docs build evaluates it exactly as shown.\n\nusing ProcessBigraphs\n\nscale = TimeScale(1, 10, :second)\ndeadline = LogicalTime(25, scale)\ndelay = Duration(5, scale)\ntarget = deadline + delay\n\n@assert physical_value(target) == 3\n@assert decode_logical_value(encode_logical_value(target)) == target\n\nresult = (\n    version=pkgversion(ProcessBigraphs),\n    logical_time=target,\n    seconds=physical_value(target),\n    identity=canonical_fingerprint((:installation_probe, target)),\n)\n\nMaterial defaults. The scale is one tenth of a second; the probe is pure and has no backend, runtime seed, or stochastic operation.\n\nExpected result. seconds == 3, logical encoding round-trips exactly, and the identity is a 64-character digest.\n\nEstablishes. This establishes that the pinned docs environment can load the package and execute exact semantic primitives.\n\nDoes not establish. It does not qualify a model, engine, backend, or scientific conclusion.\n\nBackend / runtime / seed. CPU host Julia runtime; deterministic; no seed.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/learn/install_and_verify.jl\n\nNext step. Continue to the process-bigraph mental model.","category":"section"},{"location":"api/composition-structure/#composition-structure-api","page":"Composition and structure API","title":"Composition and dynamic structure API","text":"Support level: supported internal beta.\n\nStructural effects are requests against a compiled canonical structure. The runtime validates a complete candidate, applies it atomically, and records the new structural epoch. Failed requests leave both data state and structure unchanged.\n\nUse spawn for a new admitted instance, divide when lineage and division-law semantics apply, move to change a mount relationship, and remove to retire an instance. Each operation is explicit; none silently rewires unresolved ports.\n\nBefore running a dynamic model, call validate and inspect diagram or describe. At runtime, preserve the diagnostic attached to a rejected transaction—the code and context identify the violated capacity, schema, wiring, or continuation rule.","category":"section"},{"location":"api/composition-structure/#ProcessBigraphs.spawn","page":"Composition and structure API","title":"ProcessBigraphs.spawn","text":"spawn(template, request_id, source_epoch, parent, mount_key; ...)\n\nAuthor a typed add-composite request from an admitted template. The request is pure data addressed to one source structural epoch; it does not mutate structure, create numeric state, or infer connections.\n\n\n\n\n\n","category":"function"},{"location":"api/composition-structure/#ProcessBigraphs.divide","page":"Composition and structure API","title":"ProcessBigraphs.divide","text":"divide(template, request_id, source_epoch, target, daughter_mount_key; policies, ...)\n\nAuthor a typed divide request with an explicit daughter definition and division policies. Selection and publication remain transactional; numeric inheritance, geometry, lineage, and connection behavior are never inferred.\n\n\n\n\n\n","category":"function"},{"location":"api/composition-structure/#ProcessBigraphs.remove","page":"Composition and structure API","title":"ProcessBigraphs.remove","text":"remove(request_id, source_epoch, target, owned_closure=(); ...)\n\nAuthor a typed remove request naming its source epoch and complete owned closure. Structural validation rejects incomplete closure claims and stale or unknown identities.\n\n\n\n\n\n","category":"function"},{"location":"api/composition-structure/#ProcessBigraphs.move","page":"Composition and structure API","title":"ProcessBigraphs.move","text":"move(request_id, source_epoch, target, new_parent, mount_key; ...)\n\nAuthor a typed move request between explicit structural identities. Cycles, stale generations, incompatible containment, and request conflicts fail before publication.\n\n\n\n\n\n","category":"function"},{"location":"learn/stores-ports-updates/#stores-ports-updates","page":"Stores, ports, schemas, and updates","title":"Stores, ports, schemas, and updates","text":"Support level: qualified unpublished internal beta.\n\nOutcome. Declare structured state, bind typed input and output ports, and observe additive and replacement publication without touching representation fields.\n\nPrerequisites. Your first multirate composite.","category":"section"},{"location":"learn/stores-ports-updates/#Complete-executed-source","page":"Stores, ports, schemas, and updates","title":"Complete executed source","text":"using ProcessBigraphs\nimport ProcessBigraphs: AbstractProcess, ports, semantic_version,\n    semantic_parameters, invoke\n\nstruct TypedTransfer <: AbstractProcess\n    increment::Int\nend\n\nports(::TypedTransfer) = (\n    InputPort(Int, :source; interval_behavior=:frozen),\n    OutputPort(Int, :source_out; update_law=:add),\n    OutputPort(Int, :published; update_law=:replace),\n)\nsemantic_version(::TypedTransfer) = \"1.0.0\"\nsemantic_parameters(law::TypedTransfer) = (increment=law.increment,)\nfunction invoke(law::TypedTransfer, inputs, context)\n    next = inputs[:source] + law.increment\n    InvocationResult((\n        emit(context, :source_out, AdditiveUpdate(), law.increment),\n        emit(context, :published, ReplaceUpdate(), next),\n    ))\nend\n\nscale = TimeScale(1)\nschema = BranchSchema(\n    source=LeafSchema(Int; default=1, update_law=:add, units=\"molecule\"),\n    published=LeafSchema(Int; default=0, update_law=:replace, units=\"molecule\"),\n)\nmodel = compose(:TypedStores, schema; scale) do system, stores\n    transfer = mount!(system, :transfer, TypedTransfer(2))\n    connect!(system, transfer.source, stores.source)\n    connect!(system, transfer.source_out, stores.source)\n    connect!(system, transfer.published, stores.published)\n    schedule!(system, transfer, Every(Duration(1, scale)))\nend\n\nruntime = initialize_runtime(compile(model))\nrun_until!(runtime, LogicalTime(3, scale))\nsnapshot = current_snapshot(runtime)\n\nresult = (\n    leaves=first.(schema_leaves(schema)),\n    source=snapshot[path(\"source\")],\n    published=snapshot[path(\"published\")],\n    commits=event_count(runtime),\n)\n@assert result.source == 7\n@assert result.published == 7\n\nThe process reads a frozen projection. It emits data, not mutations. The runtime validates each Delta, reconciles by the store’s declared update law, and atomically publishes the next committed snapshot.\n\nUse schema_at and schema_leaves for inspection. Concrete schema fields are representation, not model API.\n\nMaterial defaults. source begins at 1; cadence 1; additive increment 2; three logical ticks.\n\nExpected result. Both leaves contain 7 after three commits.\n\nEstablishes. Typed binding and two explicit update laws in one process.\n\nDoes not establish. It does not show conflicting replacement proposals; see logical state, effects, and reconciliation.\n\nBackend / runtime / seed. CPU serial runtime; deterministic; no RNG draw.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/learn/stores_ports_updates.jl\n\nNext step. Compose and inspect a system.","category":"section"},{"location":"learn/adapters-and-solvers/#adapters-and-solvers","page":"Integrate adapters and solvers","title":"Integrate adapters and solvers","text":"Support level: qualified SciML extension and managed-field internal beta.\n\nOutcome. Give a real SciML solver numerical ownership while ProcessBigraphs retains schedule, authorization, validation, and publication.\n\nPrerequisites. Write processes, steps, and observers.","category":"section"},{"location":"learn/adapters-and-solvers/#Complete-executed-source","page":"Integrate adapters and solvers","title":"Complete executed source","text":"using ProcessBigraphs\nusing OrdinaryDiffEqTsit5: Tsit5\n\nscale = TimeScale(1, 10, :second)\ninitial = zeros(Float64, 6, 6)\ninitial[3:4, 3:4] .= 1\n\nfield_problem = BoundedCartesianFieldProblem(\n    \"tutorial-field\",\n    initial;\n    diffusion=0.05,\n    decay=0.01,\n    tick_duration=0.1,\n    time_scale=scale,\n)\ndeclaration = sciml_field_declaration(\n    field_problem,\n    Tsit5();\n    algorithm_id=\"ordinarydiffeq-tsit5\",\n    solver_options=(abstol=1.0e-8, reltol=1.0e-8),\n)\nfield_process = managed_field_process(\n    declaration;\n    resource_authorization=(\n        backend=:cpu,\n        precision=:float64,\n        residency=:host,\n    ),\n    subcycles_per_mcs=2,\n)\n\nmodel = compose(:SolverBoundary; scale, profile=:reproducible) do system\n    field = store!(\n        system, :field,\n        LeafSchema(\n            Float64;\n            shape=size(initial),\n            default=initial,\n            update_law=:replace,\n        ),\n    )\n    forcing = store!(\n        system, :forcing,\n        LeafSchema(\n            Float64;\n            shape=size(initial),\n            default=zeros(size(initial)),\n            update_law=:replace,\n        ),\n    )\n    decay_weights = store!(\n        system, :decay_weights,\n        LeafSchema(\n            Float64;\n            shape=size(initial),\n            default=ones(size(initial)),\n            update_law=:replace,\n        ),\n    )\n    published = store!(\n        system, :published,\n        LeafSchema(\n            Float64;\n            shape=size(initial),\n            default=initial,\n            update_law=:replace,\n        ),\n    )\n    engine = mount!(system, :field_engine, field_process)\n    attach!(system, engine, (\n        field=field,\n        forcing=forcing,\n        decay_weights=decay_weights,\n        field_out=field,\n        mcs_field=published,\n    ))\n    schedule!(system, engine, Every(Duration(1, scale)))\nend\n\nruntime = initialize_runtime(compile(model))\nrun_until!(runtime, LogicalTime(2, scale))\nfield = current_snapshot(runtime)[path(\"field\")]\n\nresult = (\n    mass=sum(field),\n    minimum=minimum(field),\n    commits=event_count(runtime),\n    declaration=declaration.fingerprint,\n)\n@assert result.minimum >= 0\n@assert result.commits == 2\n\nThe selected Tsit5() algorithm, tolerances, resources, and publication cadence are visible at the call site. The solver owns integration and its private workspace. ProcessBigraphs owns invocation order and commits only a validated candidate. mcs_field publishes only on the configured two-subcycle boundary.\n\nMaterial defaults. 6×6 periodic field, diffusion 0.05, decay 0.01, 0.1-second ticks, Tsit5, abstol = reltol = 1e-8, two subcycles.\n\nExpected result. Two commits, finite nonnegative field, stable declaration fingerprint.\n\nEstablishes. A real pinned SciML handoff on CPU/Float64/host.\n\nDoes not establish. It does not claim method independence, physical calibration, GPU support, or equality to another adapter.\n\nBackend / runtime / seed. CPU, Float64, host residency; deterministic field solve; no stochastic seed.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/learn/adapters_and_solvers.jl\n\nNext step. Change structure transactionally.","category":"section"},{"location":"learn/time-scheduling-publication/#time-scheduling-publication","page":"Logical time, scheduling, and publication","title":"Logical time, scheduling, and publication","text":"Support level: qualified unpublished internal beta.\n\nOutcome. Combine an exact periodic cadence with a one-shot boundary and inspect the order of atomic publications.\n\nPrerequisites. Compose and inspect a system.","category":"section"},{"location":"learn/time-scheduling-publication/#Complete-executed-source","page":"Logical time, scheduling, and publication","title":"Complete executed source","text":"using ProcessBigraphs\nimport ProcessBigraphs: AbstractProcess, ports, semantic_version,\n    semantic_parameters, invoke\n\nstruct TimedPulse <: AbstractProcess\n    amount::Int\nend\n\nports(::TimedPulse) = (\n    OutputPort(Int, :out; update_law=:add),\n)\nsemantic_version(::TimedPulse) = \"1.0.0\"\nsemantic_parameters(pulse::TimedPulse) = (amount=pulse.amount,)\ninvoke(pulse::TimedPulse, inputs, context) = InvocationResult((\n    emit(context, :out, AdditiveUpdate(), pulse.amount),\n))\n\nscale = TimeScale(1, 100, :second)\nmodel = compose(:TimedPublication; scale) do system\n    total = store!(\n        system, :total,\n        LeafSchema(Int; default=0, update_law=:add),\n    )\n    periodic = mount!(system, :periodic, TimedPulse(1))\n    connect!(system, periodic.out, total)\n    schedule!(system, periodic, Every(Duration(2, scale)))\n    one_shot = mount!(system, :one_shot, TimedPulse(10))\n    connect!(system, one_shot.out, total)\n    schedule!(system, one_shot, At(LogicalTime(3, scale)))\nend\n\nruntime = initialize_runtime(compile(model))\nrun_until!(runtime, LogicalTime(6, scale))\n\nresult = (\n    value=current_snapshot(runtime)[path(\"total\")],\n    commits=event_count(runtime),\n    times=Tuple(record.time.tick for record in event_trace(runtime)),\n)\n@assert result.value == 13\n@assert result.times == (2, 3, 4, 6)\n\nlogical tick     0     2     3     4     6\nperiodic               +1          +1    +1\none-shot                     +10\npublished total  0     1     11    12    13\n\nAt is exhausted after its listed boundary. Every advances by exact integer ticks. Visibility changes only after reconciliation and publication; a due component cannot expose a partial update.\n\nMaterial defaults. 0.01-second ticks, periodic cadence 2, one-shot tick 3, horizon tick 6.\n\nExpected result. Publications at (2, 3, 4, 6) and final value 13.\n\nEstablishes. Exact schedule ordering and published-state visibility.\n\nDoes not establish. The declared seconds are not an empirical calibration.\n\nBackend / runtime / seed. CPU serial runtime; deterministic; no RNG draw.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/learn/time_scheduling_publication.jl\n\nNext step. Write processes, steps, and observers.","category":"section"},{"location":"concepts/canonical-identity/#canonical-identity","page":"Canonical structure and semantic identity","title":"Canonical structure and semantic identity","text":"Support level: qualified internal-beta identity contracts.\n\nIdentity is layered because “the same model” is not one question.\n\nIdentity Changes when\nScientific family/version admitted scientific meaning changes\nDocumentation profile teaching extent, seed, or runtime bound changes\nSemantic model fingerprint stores, actors, parameters, ports, or schedules change\nCanonical IR fingerprint normalized structure changes\nExecution-plan fingerprint compiled execution policy changes\nProblem fingerprint initial state, interventions, observations, horizon, or seed changes\nRun identity one execution instance is created\nCheckpoint identity persisted logical state or clocks change\n\nCanonical encodings are deterministic and construction-order invariant for semantically unordered declarations. They never rely on object addresses, anonymous closure identity, dictionary iteration order, ACSet row order, or display formatting. A component's concrete Julia type name is diagnostic and dispatch information, not scientific identity. Renaming a reconstructable type without changing the mounted name, kind, semantic version, semantic parameters, ports, capabilities, schedule, dependencies, domain, or continuation contract preserves the semantic fingerprint.\n\nsemantic_fingerprint(model), ir_fingerprint(lower(model)), and plan_fingerprint(compile(model)) should be recorded separately. Equality at one layer does not imply equality at another. In particular, canonical IR may retain the concrete type as reconstructable implementation metadata, so a type rename can preserve semantic identity while changing the IR fingerprint.","category":"section"},{"location":"concepts/canonical-identity/#Migration-rule","page":"Canonical structure and semantic identity","title":"Migration rule","text":"Persistence readers validate their schema identity and compatible semantic context. A new semantic model version does not silently claim an old checkpoint. Migration must be explicit, testable, and trace the old and new meaning.\n\nProcessBigraphs 0.6 uses authoring contract process-bigraph-authoring-v2. It removes concrete Julia component type names from semantic model fingerprints. Semantic archives written under process-bigraph-authoring-v1 are rejected with a migration-required diagnostic; they are not silently relabeled because every affected fingerprint must be recomputed and reviewed.\n\nNext: Logical state, effects, and reconciliation.","category":"section"},{"location":"#ProcessBigraphs.jl","page":"Home","title":"ProcessBigraphs.jl","text":"<div class=\"pb-hero\"><div>","category":"section"},{"location":"#Compose-mechanisms.-Keep-authority-explicit.","page":"Home","title":"Compose mechanisms. Keep authority explicit.","text":"ProcessBigraphs.jl is a domain-neutral, multirate orchestration runtime for scientific models. It gives stores, ports, schedules, reconciliation, observation, and replay one inspectable meaning while leaving heavy numerical work with solver and engine adapters.\n\nBuild your first composite · Understand the guarantees · Read the API by task\n\n</div><div>\n\n(Image: Authoring lowers to a canonical structure and execution plan; the runtime schedules an engine adapter and publishes observable state.)\n\n</div></div>\n\nSupport level — qualified unpublished internal beta. This manual documents the admitted ProcessBigraphs 0.6.0 boundary on this repository branch. It is not a registry release or a complete implementation of every Process-Bigraph 2.0 concept.","category":"section"},{"location":"#Choose-a-route","page":"Home","title":"Choose a route","text":"<div class=\"pb-grid\"><div class=\"pb-card\">","category":"section"},{"location":"#Compose-a-model","page":"Home","title":"Compose a model","text":"Start with the mental model, then build a multirate composite. Every connection and schedule stays visible.\n\n</div><div class=\"pb-card\">","category":"section"},{"location":"#Integrate-an-engine","page":"Home","title":"Integrate an engine","text":"Learn the adapter boundary and implement the public extension protocol.\n\n</div><div class=\"pb-card\">","category":"section"},{"location":"#Reproduce-a-run","page":"Home","title":"Reproduce a run","text":"Follow RNG, observation, checkpoints, and replay, then audit the source-bounded scientific case studies.\n\n</div></div>","category":"section"},{"location":"#What-the-package-owns","page":"Home","title":"What the package owns","text":"ProcessBigraphs owns logical time, scheduling, typed projections, effect reconciliation, atomic publication, structural transaction boundaries, semantic RNG addressing, observation, and logical checkpoint/replay. An adapter owns numerical kernels, solver state, buffers, devices, and the method used to produce a candidate.\n\nThat split is a correctness boundary: a completed engine operation is not visible state until ProcessBigraphs validates and publishes its candidate.","category":"section"},{"location":"#A-complete-authoring-shape","page":"Home","title":"A complete authoring shape","text":"model = compose(:CoupledExperiment; scale=TimeScale(1)) do system\n    signal = store!(\n        system, :signal,\n        LeafSchema(Float64; default=0.0, update_law=:add),\n    )\n    pulse = mount!(system, :pulse, MyPulse(0.25))\n    connect!(system, pulse.signal, signal)\n    schedule!(system, pulse, Every(Duration(1, TimeScale(1))))\n    observable!(system, :signal, signal)\nend\n\nreport = validate(model)\nplan = compile(model)\nproblem = SimulationProblem(model; seed=42)\n\nThere is no name-matching autowire step. Mounting does not connect, schedule, or expose a component.","category":"section"},{"location":"#Scientific-boundary","page":"Home","title":"Scientific boundary","text":"The Wortel and Merks pages are qualified source-bounded case studies. They show reduced deterministic teaching profiles and explicit nonclaims. A successful bounded run does not reproduce a paper figure, estimate an ensemble, validate physical time, or imply author endorsement.","category":"section"},{"location":"#Reproduce-this-site","page":"Home","title":"Reproduce this site","text":"julia --project=lib/ProcessBigraphs/docs -e 'using Pkg; Pkg.instantiate()'\njulia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/make.jl\n\nThe build is strict: doctests execute, cross-reference warnings fail, and the curated navigation contains exactly the registered 35 pages.","category":"section"},{"location":"api/extension-experimental/#extension-experimental-api","page":"Extension protocols, experimental surface, and compatibility","title":"Extension and experimental API","text":"Support level: qualified extension API for the listed protocol; everything else on this page is experimental internal beta.\n\nExtension bindings are Julia-public and intentionally unexported. Adapter modules should import exactly the names they implement:\n\nimport ProcessBigraphs\nimport ProcessBigraphs: AbstractEngineAdapter, AbstractEngineInstance,\n    AbstractCompletionHandle, EngineCandidate, prepare_engine,\n    stage_operation!, complete_operation!, validate_candidate,\n    publish_candidate!, discard_candidate!\n\nThis makes the dependency visible in source and prevents protocol names from crowding ordinary model authoring.","category":"section"},{"location":"api/extension-experimental/#Component-protocol","page":"Extension protocols, experimental surface, and compatibility","title":"Component protocol","text":"Component methods describe semantic declarations. Runtime caches, sessions, and buffers must not appear in semantic_parameters.","category":"section"},{"location":"api/extension-experimental/#Engine-declaration-and-operations","page":"Extension protocols, experimental surface, and compatibility","title":"Engine declaration and operations","text":"An invocation contains immutable projections and explicit resource authorization. An adapter stages work privately, then returns a candidate; only ProcessBigraphs decides whether and when that candidate may publish.","category":"section"},{"location":"api/extension-experimental/#Required-call-order","page":"Extension protocols, experimental surface, and compatibility","title":"Required call order","text":"prepare_engine constructs implementor-owned execution state.\nstage_operation! starts or stages a candidate without publication.\ncomplete_operation! resolves the completion handle.\nvalidate_candidate checks time, outputs, diagnostics, and declaration compatibility.\npublish_candidate! atomically installs an authorized candidate.\ndiscard_candidate! cleans up every rejected or failed candidate.\n\nIf staging, completion, validation, authorization, or publication fails, the previously published engine state remains authoritative. An adapter must make discard_candidate! safe for every candidate it can stage.\n\nConcrete sessions, solver integrators, workspaces, caches, buffers, streams, tasks, and device allocations are never part of the extension contract.","category":"section"},{"location":"api/extension-experimental/#ProcessBigraphs.AbstractProcess","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.AbstractProcess","text":"AbstractProcess\n\nSupertype for scheduled temporal components. Implementors provide typed ports, stable semantic identity, and invoke; every mounted process requires an explicit temporal schedule.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.AbstractStep","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.AbstractStep","text":"AbstractStep\n\nSupertype for zero-time reactive components. Steps participate in compiled acyclic or explicitly bounded iteration layers and emit typed effects against one committed event boundary.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.AbstractObserver","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.AbstractObserver","text":"AbstractObserver\n\nSupertype for typed external observers. Implementors receive only declared state projections and an isolated semantic RNG context, and return validated records that cannot mutate logical model state.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.ports","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.ports","text":"ports(component)\n\nReturn a reconstructable tuple of typed input and output port declarations for a process or step. Port name, direction, value type, interval behavior, update law, cardinality, and residency participate in validation and identity.\n\n\n\n\n\n","category":"function"},{"location":"api/extension-experimental/#ProcessBigraphs.capabilities","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.capabilities","text":"capabilities(component)\n\nReturn the component’s declared capability requirements. The declaration is a preflight envelope, not evidence that every backend or method supported by a dependency is qualified.\n\n\n\n\n\n","category":"function"},{"location":"api/extension-experimental/#ProcessBigraphs.semantic_version","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.semantic_version","text":"semantic_version(component)\n\nReturn the stable semantic version string for a process or step implementation. Changing behavior without changing the version invalidates canonical identity and persistence assumptions.\n\n\n\n\n\n","category":"function"},{"location":"api/extension-experimental/#ProcessBigraphs.semantic_parameters","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.semantic_parameters","text":"semantic_parameters(component)\n\nReturn a canonical-encodable named tuple containing every behavior-affecting semantic parameter of a process or step. Runtime caches, buffers, and display settings must not appear here.\n\n\n\n\n\n","category":"function"},{"location":"api/extension-experimental/#ProcessBigraphs.invoke","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.invoke","text":"invoke(component, inputs, context) -> InvocationResult\n\nExecute one authorized process or step activation against immutable typed inputs. Return deltas, diagnostics, an optional continuation, and allowed requests; do not mutate committed stores or forge producer/event identities.\n\n\n\n\n\n","category":"function"},{"location":"api/extension-experimental/#ProcessBigraphs.observe","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.observe","text":"observe(observer, projection, context) -> ObservationResult\n\nProduce one typed record from the observer’s authorized projection. Observation uses an isolated RNG namespace and may advance only its declared continuation; it cannot emit model-state effects.\n\n\n\n\n\n","category":"function"},{"location":"api/extension-experimental/#ProcessBigraphs.observer_semantic_version","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.observer_semantic_version","text":"observer_semantic_version(observer)\n\nReturn the stable semantic version string for an observer implementation. Record meaning or continuation behavior changes require an explicit version and migration decision.\n\n\n\n\n\n","category":"function"},{"location":"api/extension-experimental/#ProcessBigraphs.observer_semantic_parameters","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.observer_semantic_parameters","text":"observer_semantic_parameters(observer)\n\nReturn every behavior-affecting observer parameter as a canonical-encodable named tuple. Presentation-only configuration and mutable caches are excluded.\n\n\n\n\n\n","category":"function"},{"location":"api/extension-experimental/#ProcessBigraphs.observer_continuation_schema","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.observer_continuation_schema","text":"observer_continuation_schema(observer)\n\nReturn the observer’s typed continuation schema and codec contract. Stateless observers use stateless_continuation_schema; incompatible persisted continuations fail restore.\n\n\n\n\n\n","category":"function"},{"location":"api/extension-experimental/#ProcessBigraphs.AbstractEngineAdapter","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.AbstractEngineAdapter","text":"AbstractEngineAdapter\n\nSupertype for reconstructable, immutable engine declarations. An adapter names the problem and algorithm semantics it supports; mutable solver state belongs to an AbstractEngineInstance.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.AbstractEngineInstance","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.AbstractEngineInstance","text":"AbstractEngineInstance\n\nSupertype for implementor-owned mutable engine sessions, workspaces, buffers, integrators, or device state. Instances stage candidates but cannot make them logical state without runtime validation and publication.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.AbstractEngineOperation","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.AbstractEngineOperation","text":"AbstractEngineOperation\n\nSupertype for typed authorized engine operations such as IntervalAdvance, BoundarySolve, and DiscreteBatch.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.AbstractCompletionHandle","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.AbstractCompletionHandle","text":"AbstractCompletionHandle\n\nSupertype for implementor-owned handles returned by stage_operation!. A handle may represent immediate, asynchronous, device, or solver completion; it is consumed by complete_operation!.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.EngineCapabilities","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.EngineCapabilities","text":"EngineCapabilities(...)\n\nAn immutable, envelope-specific declaration. A capability is evidence scope, not a claim that every method dispatch or backend supported by a dependency is qualified.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.EngineDeclaration","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.EngineDeclaration","text":"EngineDeclaration(id, adapter; semantic_version, capabilities, parameters)\n\nBind a reconstructable adapter to an explicit semantic version, canonical parameters, and a narrow capability envelope. The declaration fingerprint authorizes later invocations; mutable instance state is not retained.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.IntervalAdvance","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.IntervalAdvance","text":"IntervalAdvance(start_time, target_time)\n\nAuthorize an engine to advance over one positive exact logical interval. Adapters must reach target_time for a successful terminal candidate or return a typed nonterminal outcome within the interval.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.BoundarySolve","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.BoundarySolve","text":"BoundarySolve(time, problem)\n\nAuthorize one named zero-duration boundary problem at an exact logical time. The problem symbol and operation family must fall within the engine’s declared capability envelope.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.DiscreteBatch","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.DiscreteBatch","text":"DiscreteBatch(time, batch, count=1)\n\nAuthorize a positive finite count of named discrete operations at one exact logical boundary. Batch identity and count are semantic invocation inputs.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.EngineInputProjection","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.EngineInputProjection","text":"EngineInputProjection(name, version, time, value; mode=:frozen)\n\nCapture an immutable, canonical-encoded, versioned engine input projection. Projection time cannot be after invocation start, and its mode must be admitted by the engine declaration.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.EngineInvocation","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.EngineInvocation","text":"EngineInvocation(id, reason, declaration, operation; ...)\n\nDescribe one authorized engine handoff: operation, structural epoch, immutable inputs, RNG context, selected resources, and expected effect/diagnostic schemas. Construction rejects requests outside declared capabilities.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.EngineCandidate","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.EngineCandidate","text":"EngineCandidate(actual_time, payload; effects, continuation, diagnostics, fingerprint)\n\nRepresent a completed but not-yet-published engine result. A candidate remains implementor-owned until the runtime validates reached time, output schemas, diagnostics, authorization, and policy.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.EngineEarlyReturn","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.EngineEarlyReturn","text":"EngineEarlyReturn(actual_time, reason; diagnostics)\n\nReport a nonterminal return strictly within an authorized interval. A result at the exact terminal target must be an EngineCandidate, not an early return.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.EngineEventRequest","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.EngineEventRequest","text":"EngineEventRequest(actual_time, event, payload=NamedTuple())\n\nRequest a typed runtime-visible event at a nonterminal authorized time. The runtime validates temporal bounds and decides how the request affects scheduling; the adapter does not publish state directly.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.EngineFailure","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.EngineFailure","text":"EngineFailure(code, stage; retry_class=:never, diagnostics)\n\nReturn a structured engine failure with an explicit retry class. Managed execution discards staged candidates and preserves the pre-publication logical state before reporting the failure.\n\n\n\n\n\n","category":"type"},{"location":"api/extension-experimental/#ProcessBigraphs.projection_value","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.projection_value","text":"projection_value(input)\n\nDecode and return an owned value from an immutable EngineInputProjection. Adapters should use this accessor rather than depend on the projection’s canonical byte representation.\n\n\n\n\n\n","category":"function"},{"location":"api/extension-experimental/#ProcessBigraphs.prepare_engine","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.prepare_engine","text":"prepare_engine(adapter, declaration) -> AbstractEngineInstance\nprepare_engine(declaration) -> AbstractEngineInstance\n\nConstruct implementor-owned mutable engine state for a declaration. Verify that the adapter and declaration agree and allocate only resources authorized by the declared capability and later invocation policy.\n\n\n\n\n\n","category":"function"},{"location":"api/extension-experimental/#ProcessBigraphs.stage_operation!","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.stage_operation!","text":"stage_operation!(instance, invocation) -> AbstractCompletionHandle\n\nValidate adapter-specific inputs and resources, perform or launch heavy work, and stage a private candidate without publishing it. Only one active candidate may own a given invocation identity unless the adapter documents otherwise.\n\n\n\n\n\n","category":"function"},{"location":"api/extension-experimental/#ProcessBigraphs.complete_operation!","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.complete_operation!","text":"complete_operation!(instance, handle)\n\nWait for or collect a staged operation and return exactly one EngineCandidate, EngineEarlyReturn, EngineEventRequest, or EngineFailure. Completion alone does not publish logical state.\n\n\n\n\n\n","category":"function"},{"location":"api/extension-experimental/#ProcessBigraphs.validate_candidate","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.validate_candidate","text":"validate_candidate(instance, invocation, candidate) -> true\n\nValidate terminal time, exact effect names, and exact diagnostic schema for a candidate. Adapter overloads may add domain checks but must preserve the base runtime contract and fail before publication.\n\n\n\n\n\n","category":"function"},{"location":"api/extension-experimental/#ProcessBigraphs.publish_candidate!","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.publish_candidate!","text":"publish_candidate!(instance, invocation, candidate)\n\nPerform the adapter’s one-way publication after runtime validation and authorization. The method must verify candidate ownership and return a canonical-encodable publication summary.\n\n\n\n\n\n","category":"function"},{"location":"api/extension-experimental/#ProcessBigraphs.discard_candidate!","page":"Extension protocols, experimental surface, and compatibility","title":"ProcessBigraphs.discard_candidate!","text":"discard_candidate!(instance, invocation, outcome)\n\nRelease or reset all private staged state associated with an invocation after pre-publication failure or a nonterminal outcome. Cleanup must not mutate committed logical state.\n\n\n\n\n\n","category":"function"},{"location":"case-studies/merks-2006/#merks-2006-case","page":"Merks 2006","title":"Merks 2006","text":"Content class: qualified source-bounded case study. Support level: qualified unpublished internal beta.\n\nOutcome. Assemble the reduced Merks vasculogenesis mechanism from ordinary PottsToolkit declarations and an explicit multirate ProcessBigraph coupling CPM, secretion, diffusion, decay, and typed observations.\n\nPrerequisites. Adapters and solvers, write components, and Cellular Potts concepts.","category":"section"},{"location":"case-studies/merks-2006/#Source,-ambiguity-trace,-and-profiles","page":"Merks 2006","title":"Source, ambiguity trace, and profiles","text":"Source: Merks et al. (2006), DOI 10.1016/j.ydbio.2005.10.003. The trace covers equations 3–6 and the explicit software choices for periodic field boundaries, Moore local-connectivity rejection, and deterministic nonoverlapping placement.\n\nChoice Canonical profile Executed docs profile\nDomain 500×500 20×20\nCells 282 2\nInitially occupied sites 28,200 10\nTarget area 100 5\nTarget length 50 5\nTemperature 50 50\nChemotaxis γ 1000 1000\nField subcycles / MCS 15 15\nHorizon bounded qualification 2 MCS\nSeed declared canonical 11\n\nThe high-level biological model is shared exactly; domain, population, targets, runtime, and seed are intentional documentation reductions.","category":"section"},{"location":"case-studies/merks-2006/#Complete-executed-source","page":"Merks 2006","title":"Complete executed source","text":"The following is the entire program. No reference-model constructor or hidden scientific setup is used.\n\nusing PottsToolkit\nusing OrdinaryDiffEqTsit5: Tsit5\nimport CorePotts\nimport ProcessBigraphs as PB\nimport SciMLBase\n\nconst MERKS_SOURCE_DOI = \"10.1016/j.ydbio.2005.10.003\"\nconst MERKS_SEMANTIC_VERSION = \"2.0.0\"\n\nprofile = (\n    identity=:reduced_docs_cpu,\n    dimensions=(20, 20),\n    cells=2,\n    central_extent=16,\n    target_area_sites=5.0,\n    target_length_sites=5.0,\n    temperature=50.0,\n    chemotaxis_gamma=1000.0,\n    volume_strength=50.0,\n    source_length_strength=5.0,\n    secretion_rate=1.8e-4,\n    diffusion=0.1,\n    decay=1.8e-4,\n    subcycles_per_mcs=15,\n    mcs=2,\n    seed=UInt64(11),\n)\n\ncell = CellType(:endothelial)\nextracellular = Medium(:extracellular_matrix)\nborder = Medium(:border)\nchemoattractant = Field(\n    :chemoattractant;\n    placement=CellCentered(),\n    boundary=PeriodicField(),\n    interpolation=Nearest(),\n)\nmoore = CorePotts.MooreTopology{2}()\nspatial_roles = CorePotts.SpatialRoles(\n    proposal=CorePotts.static_relation(CorePotts.ProposalRole(), moore),\n    contact=CorePotts.static_relation(CorePotts.ContactRole(), moore),\n    surface=CorePotts.static_relation(CorePotts.SurfaceRole(), moore),\n    connectivity=CorePotts.static_relation(CorePotts.ConnectivityRole(), moore),\n    query=CorePotts.static_relation(CorePotts.SpatialQueryRole(), moore),\n)\npotts_model = PottsModel(\n    cell,\n    extracellular,\n    border,\n    Volume(\n        cell => (\n            target=profile.target_area_sites,\n            strength=profile.volume_strength,\n        ),\n    ),\n    Elongation(\n        cell => (\n            target=profile.target_length_sites / 4,\n            strength=16 * profile.source_length_strength,\n        );\n        target_division=CloneOnDivision(),\n    ),\n    Adhesion(PairwiseLaw(\n        :contact_energy,\n        (cell, cell) => 40.0,\n        (cell, extracellular) => 20.0,\n        (cell, border) => 100.0,\n        (extracellular, extracellular) => 0.0,\n        (extracellular, border) => 0.0,\n        (border, border) => 0.0,\n    )),\n    chemoattractant,\n    Chemotaxis(\n        chemoattractant,\n        cell => profile.chemotaxis_gamma / profile.temperature;\n        response=LinearResponse(),\n        mode=ExtensionChemotaxis(),\n    ),\n    LocalConnectivity(),\n    spatial_roles,\n)\n\nfunction initial_merks_labels(profile)\n    labels = zeros(UInt64, profile.dimensions)\n    grid_width = ceil(Int, sqrt(profile.cells))\n    lower = ntuple(axis ->\n        fld(profile.dimensions[axis] - profile.central_extent, 2) + 1, 2)\n    spacing = profile.central_extent / grid_width\n    radius = sqrt(profile.target_area_sites / pi)\n    identity = 0\n    for gy in 1:grid_width, gx in 1:grid_width\n        identity == profile.cells && break\n        identity += 1\n        center = (\n            lower[1] - 1 + (gx - 0.5) * spacing,\n            lower[2] - 1 + (gy - 0.5) * spacing,\n        )\n        bound = ceil(Int, radius) + 1\n        xbounds = (\n            max(1, floor(Int, center[1]) - bound):\n            min(profile.dimensions[1], ceil(Int, center[1]) + bound)\n        )\n        ybounds = (\n            max(1, floor(Int, center[2]) - bound):\n            min(profile.dimensions[2], ceil(Int, center[2]) + bound)\n        )\n        candidates = [\n            CartesianIndex(x, y) for y in ybounds for x in xbounds\n        ]\n        sort!(candidates; by=site -> (\n            sum((site[axis] - center[axis])^2 for axis in 1:2),\n            Tuple(site),\n        ))\n        for site in candidates[1:round(Int, profile.target_area_sites)]\n            iszero(labels[site]) ||\n                throw(ArgumentError(\"initial cells overlap\"))\n            labels[site] = UInt64(identity)\n        end\n    end\n    labels\nend\n\nlabels = initial_merks_labels(profile)\ninitial_field = zeros(Float64, profile.dimensions)\nfield_scale = PB.TimeScale(2, 1, :second)\nfield_problem = PB.BoundedCartesianFieldProblem(\n    \"merks-chemoattractant\",\n    initial_field;\n    spacing=(2.0, 2.0),\n    diffusion=profile.diffusion,\n    decay=profile.decay,\n    tick_duration=2.0,\n    time_scale=field_scale,\n)\nfield_declaration = PB.sciml_field_declaration(\n    field_problem,\n    Tsit5();\n    algorithm_id=\"ordinarydiffeq-tsit5\",\n    solver_options=(abstol=1.0e-9, reltol=1.0e-9),\n)\nfield_process = PB.managed_field_process(\n    field_declaration;\n    resource_authorization=(\n        backend=:cpu,\n        precision=:float64,\n        residency=:host,\n    ),\n    subcycles_per_mcs=profile.subcycles_per_mcs,\n)\n\nfunction merks_problem(\n        potts_model, cell, chemoattractant, profile, labels, field, mcs)\n    identities = Tuple(\n        id => cell for id in sort!(collect(Set(filter(!iszero, labels))))\n    )\n    PottsProblem(\n        potts_model,\n        CartesianDomain(\n            profile.dimensions;\n            spacing=(1.0, 1.0),\n            boundaries=(\n                AxisBoundary(ClosedBoundary()),\n                AxisBoundary(ClosedBoundary()),\n            ),\n        ),\n        Layout(LabelledCells(labels, identities));\n        fields=(chemoattractant => field,),\n        capacity=profile.cells,\n        tspan=(mcs - 1, mcs),\n        seed=profile.seed,\n    )\nend\n\nfunction logical_labels(state, dimensions)\n    labels = zeros(UInt64, dimensions)\n    for site in eachindex(labels)\n        owner = CorePotts.owner_at(state, site)\n        labels[site] = CorePotts.is_cell_owner(owner) ?\n            UInt64(CorePotts.value(CorePotts.cell_id(owner))) : UInt64(0)\n    end\n    labels\nend\n\nstruct MerksCPMStep{M,C,F,P} <: PB.AbstractStep\n    model::M\n    cell::C\n    field::F\n    profile::P\nend\n\nPB.ports(::MerksCPMStep) = (\n    PB.PortSpec(Array{UInt64,2}, :labels, :input;\n        interval_behavior=:event_updated),\n    PB.PortSpec(Array{Float64,2}, :mcs_field, :input;\n        interval_behavior=:event_updated),\n    PB.PortSpec(Array{UInt64,2}, :labels_out, :output;\n        update_law=:replace),\n)\nPB.semantic_version(::MerksCPMStep) = MERKS_SEMANTIC_VERSION\nPB.semantic_parameters(step::MerksCPMStep) = (\n    family=:Merks2006,\n    source_doi=MERKS_SOURCE_DOI,\n    profile=step.profile.identity,\n    potts_fingerprint=semantic_fingerprint(step.model).digest,\n)\nfunction PB.invoke(step::MerksCPMStep, inputs, context)\n    mcs = Int(div(context.end_time.tick, step.profile.subcycles_per_mcs))\n    experiment = merks_problem(\n        step.model,\n        step.cell,\n        step.field,\n        step.profile,\n        inputs[:labels],\n        inputs[:mcs_field],\n        mcs,\n    )\n    integrator = SciMLBase.init(\n        experiment,\n        SequentialCPM(temperature=step.profile.temperature),\n    )\n    SciMLBase.step!(integrator)\n    updated = logical_labels(\n        CorePotts.logical_state(integrator),\n        step.profile.dimensions,\n    )\n    PB.InvocationResult((\n        PB.emit(context, :labels_out, PB.ReplaceUpdate(), updated),\n    ); diagnostics=(\n        mcs,\n        accepted_copies=CorePotts.current_mcs_report(integrator).accepted_copies,\n    ))\nend\n\nstruct MerksSecretion <: PB.AbstractStep\n    rate::Float64\nend\n\nPB.ports(::MerksSecretion) = (\n    PB.PortSpec(Array{UInt64,2}, :labels, :input;\n        interval_behavior=:event_updated),\n    PB.PortSpec(Array{Float64,2}, :forcing, :output;\n        update_law=:replace),\n    PB.PortSpec(Array{Float64,2}, :decay_weights, :output;\n        update_law=:replace),\n)\nPB.semantic_version(::MerksSecretion) = MERKS_SEMANTIC_VERSION\nPB.semantic_parameters(step::MerksSecretion) = (\n    family=:Merks2006,\n    source_equation=6,\n    secretion_rate=step.rate,\n)\nfunction PB.invoke(step::MerksSecretion, inputs, context)\n    labels = inputs[:labels]\n    forcing = map(label -> iszero(label) ? 0.0 : step.rate, labels)\n    decay_weights = map(label -> iszero(label) ? 1.0 : 0.0, labels)\n    PB.InvocationResult((\n        PB.emit(context, :forcing, PB.ReplaceUpdate(), forcing),\n        PB.emit(context, :decay_weights, PB.ReplaceUpdate(), decay_weights),\n    ))\nend\n\nforcing = map(\n    label -> iszero(label) ? 0.0 : profile.secretion_rate,\n    labels,\n)\ndecay_weights = map(label -> iszero(label) ? 1.0 : 0.0, labels)\ncomposite_model = PB.compose(\n    :Merks2006;\n    scale=field_scale,\n    profile=:reproducible,\n) do system\n    label_store = PB.store!(system, :labels, PB.LeafSchema(\n        UInt64; shape=profile.dimensions, default=labels,\n        update_law=:replace, ontology=\"CPM cell identity\"))\n    field_store = PB.store!(system, :field, PB.LeafSchema(\n        Float64; shape=profile.dimensions, default=initial_field,\n        update_law=:replace, units=\"concentration\"))\n    mcs_field_store = PB.store!(system, :mcs_field, PB.LeafSchema(\n        Float64; shape=profile.dimensions, default=initial_field,\n        update_law=:replace, units=\"concentration\"))\n    forcing_store = PB.store!(system, :forcing, PB.LeafSchema(\n        Float64; shape=profile.dimensions, default=forcing,\n        update_law=:replace, units=\"concentration/second\"))\n    decay_store = PB.store!(system, :decay_weights, PB.LeafSchema(\n        Float64; shape=profile.dimensions, default=decay_weights,\n        update_law=:replace))\n\n    field = PB.mount!(system, :field_solver, field_process)\n    PB.attach!(system, field, (\n        field=field_store,\n        forcing=forcing_store,\n        decay_weights=decay_store,\n        field_out=field_store,\n        mcs_field=mcs_field_store,\n    ))\n    PB.schedule!(system, field, PB.Every(PB.Duration(1, field_scale)))\n\n    cpm = PB.mount!(\n        system,\n        :cellular_potts,\n        MerksCPMStep(potts_model, cell, chemoattractant, profile),\n    )\n    PB.attach!(system, cpm, (\n        labels=label_store,\n        mcs_field=mcs_field_store,\n        labels_out=label_store,\n    ))\n\n    secretion = PB.mount!(\n        system,\n        :secretion_mask,\n        MerksSecretion(profile.secretion_rate),\n    )\n    PB.attach!(system, secretion, (\n        labels=label_store,\n        forcing=forcing_store,\n        decay_weights=decay_store,\n    ))\n    PB.schedule!(system, secretion, PB.After(cpm))\n    PB.observable!(system, :labels, label_store)\n    PB.observable!(system, :field, field_store)\nend\n\nstruct MerksSummary <: PB.AbstractObserver\n    subcycles_per_mcs::Int\nend\nPB.observer_semantic_version(::MerksSummary) = MERKS_SEMANTIC_VERSION\nPB.observer_semantic_parameters(observer::MerksSummary) = (\n    family=:Merks2006,\n    subcycles_per_mcs=observer.subcycles_per_mcs,\n)\nfunction PB.observe(observer::MerksSummary, projection, context)\n    labels = projection[PB.path(\"labels\")]\n    field = projection[PB.path(\"field\")]\n    PB.ObservationResult((\n        mcs=Int(div(context.time.tick, observer.subcycles_per_mcs)),\n        cells=length(Set(filter(!iszero, labels))),\n        occupied=count(!iszero, labels),\n        field_mass=sum(field),\n        field_minimum=minimum(field),\n        field_maximum=maximum(field),\n    ))\nend\n\nobserver = PB.ObserverSpec(\n    \"merks-state-summary\",\n    MerksSummary(profile.subcycles_per_mcs),\n    (PB.path(\"labels\"), PB.path(\"field\")),\n    PB.PeriodicObservationSchedule(\n        PB.Duration(profile.subcycles_per_mcs, field_scale),\n    );\n    record_schema=PB.RecordSchema(\n        NamedTuple; identity=\"merks-state-summary-v2\"),\n)\nruntime = PB.initialize_runtime(\n    PB.compile(composite_model),\n    PB.SerialExecutor(\n        root_seed=profile.seed,\n        observation_plan=PB.ObservationPlan((observer,)),\n    ),\n)\nhorizon = PB.LogicalTime(\n    profile.mcs * profile.subcycles_per_mcs,\n    field_scale,\n)\nPB.run_until!(runtime, horizon)\n\nrecords = Tuple(record.payload for record in PB.observation_records(runtime))\nresult = (\n    final=last(records),\n    model_fingerprint=semantic_fingerprint(potts_model).digest,\n    composite_fingerprint=PB.semantic_fingerprint(composite_model),\n    source_doi=MERKS_SOURCE_DOI,\n)\n@assert length(records) == profile.mcs\n@assert 0 < result.final.cells <= profile.cells\n@assert result.final.occupied > 0\n@assert result.final.field_mass > 0\n@assert result.final.field_minimum >= 0","category":"section"},{"location":"case-studies/merks-2006/#State,-trace,-and-inspection-loop","page":"Merks 2006","title":"State, trace, and inspection loop","text":"(Image: A paper-inspired native MakiePotts time strip shows nine endothelial cells and their chemoattractant field at MCS 0, 30, and 60, using red cell boundaries and green field contours.)\n\n(Image: A native Makie trace shows field mass and surviving cells across the longer fixed-seed Merks visualization profile, sampled every five MCS through MCS 60.)\n\n<div class=\"pb-native-animation\">\n<video autoplay loop muted playsinline controls preload=\"metadata\"\n       width=\"1800\" height=\"820\"\n       poster=\"../../assets/merks-state.png\"\n       aria-label=\"Native MakiePotts animation of the fixed-seed reduced Merks trajectory from initialization through MCS sixty\">\n  <source src=\"../../assets/merks-animation.mp4\" type=\"video/mp4\">\n  <a href=\"../../assets/merks-animation.mp4\">Download the Merks Makie animation.</a>\n</video>\n<img class=\"pb-animation-fallback\"\n     src=\"../../assets/merks-state.png\"\n     alt=\"Static MakiePotts fallback showing nine Merks cells and their chemoattractant field at MCS zero, thirty, and sixty.\">\n</div>\n\nText alternative for the animation: thirteen native MakiePotts states sample a fixed-seed 40 × 40, nine-cell visualization profile every 5 MCS through MCS 60. Red cell boundaries remain visible over the grayscale chemoattractant field; green contours reveal the evolving local and shared field structure. Reduced-motion users receive the three-panel MCS 0/30/60 time strip. The executed two-MCS program above remains the compact, fully displayed tutorial and typed-record authority. This longer profile is a paper-inspired mechanism visualization, not a reproduction of the paper’s lacuna, branch-length, or ensemble morphometry.","category":"section"},{"location":"case-studies/merks-2006/#What-this-establishes","page":"Merks 2006","title":"What this establishes","text":"The displayed program establishes:\n\nexplicit volume, elongation, adhesion, field, chemotaxis, local-connectivity, and Moore spatial-role declarations;\ndeterministic nonoverlapping placement for the reduced two-cell profile;\nexplicit stores and wiring for labels, field, MCS-visible field, forcing, and decay weights;\na real Tsit5 field adapter with visible tolerances and CPU authorization;\n15 field publications per CPM step, followed by secretion-mask refresh;\ntyped cell-count, occupied-site, and field-range/mass observations; and\nexact Potts semantic identity and fixed-seed bounded outputs with the packaged reduced Merks model.\n\nThe CPM is stochastic. Exact equality here means that the displayed and packaged assemblies use the same seed, MCS clock, initial state, and solver path. It is not evidence that one trajectory characterizes the output distribution.","category":"section"},{"location":"case-studies/merks-2006/#What-this-does-not-establish","page":"Merks 2006","title":"What this does not establish","text":"It does not reproduce Figure 5, perform morphometry, estimate an ensemble or distribution, validate the canonical 500×500/282-cell run from this page, establish physical time calibration, prove solver independence, or imply source-author endorsement. In this small fixed-seed trajectory, one initially five-site identity has no occupied sites by MCS 2; that observation is not a cell-survival conclusion.\n\nMaterial defaults. Every scientific and execution value appears in profile, the field declaration, or the explicit CPM algorithm call.\n\nExpected result. Two typed observations; cell counts 2 then 1; occupied site counts 5 then 1; increasing, nonnegative field mass; and stable model/composite identities for seed 11.\n\nBackend / runtime / seed. CPU/Float64/host; 30 field ticks = 2 bounded MCS; seed 11.\n\nReproduction command. julia --project=lib/ProcessBigraphs/docs lib/ProcessBigraphs/docs/models/case_studies/merks_2006.jl\n\nFull qualification command. julia --project=. -e 'using Pkg; Pkg.test(\"PottsToolkit\")'\n\nNext step. Audit engines and compute ownership or return to the case-study boundary.","category":"section"}]
}
